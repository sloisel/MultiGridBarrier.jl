var documenterSearchIndex = {"docs":
[{"category":"section","location":"jump/#Modeling-with-JuMP","page":"JuMP","text":"MultiGridBarrierJuMP lets you state convex variational problems in JuMP syntax and solve them with the MultiGridBarrier multigrid interior-point method. It is a JuMP.AbstractModel extension: the standard macros (@variable, @constraint, @objective) and accessors (value, objective_value, termination_status) work unchanged, but no MOI model is ever built — optimize! lowers the model directly to the classical pipeline amg → assemble → mgb_solve. The AMG hierarchy is constructed automatically from the geometry and the Dirichlet constraints; it is never user-visible.\n\nnote: Status\nWorking draft, not yet a registered package. It ships in the jump/ directory of the repository and is loaded with include. All six Zoo problems restated in JuMP syntax reproduce the classical constructors' solutions bit-for-bit (or to one ulp); the regression suite is jump/test_zoo.jl, and the full modeling reference is jump/README.md.","title":"Modeling with JuMP"},{"category":"section","location":"jump/#Setup","page":"JuMP","text":"JuMP is not a dependency of MultiGridBarrier — add it to your environment (pkg> add JuMP), then:\n\nusing MultiGridBarrier, JuMP, PyPlot\ninclude(joinpath(dirname(dirname(pathof(MultiGridBarrier))),\n                 \"jump\", \"MultiGridBarrierJuMP.jl\"))\nusing .MultiGridBarrierJuMP\nnothing # hide","title":"Setup"},{"category":"section","location":"jump/#Quick-tour:-the-p-Laplacian","page":"JuMP","text":"min int tfrac12 u + s  dx\nquad textst quad s geq nabla u^15\nqquad u = x_1^2 + x_2^2 text on  partialOmega \n\nA model is built over a fixed discretization, so every piece of spatial data (Coef) is evaluated at the quadrature nodes at modeling time. Derivatives are written deriv(u, :dx) where the symbol is a key of geom.operators; the epigraph cone [q...; slack] in EpiPower(p) means slack ≥ ‖q‖₂ᵖ pointwise (slack last).\n\ngeom = subdivide(fem2d_P2(), 2)\nm = MGBModel(geom)\nset_attribute(m, \"verbose\", false)\n@variable(m, u)                          # conforming (inferred)\n@variable(m, s, Broken())                # broken slack: one dof per node\nset_start(u, x -> x[1]^2 + x[2]^2)       # initial iterate & Dirichlet lift\nset_start(s, 100.0)\n@constraint(m, u == Coef(m, x -> x[1]^2 + x[2]^2), On(find_boundary(geom)))\n@constraint(m, [deriv(u, :dx); deriv(u, :dy); s] in EpiPower(1.5))\n@objective(m, Min, integral(Coef(m, 0.5) * u + s))\noptimize!(m)\ntermination_status(m)\n\nplot(mgb_solution(m)); savefig(\"jump_plaplace.svg\"); nothing  # hide\nclose()  # hide\n\n(Image: )\n\nThis is exactly the package's default problem, so we can compare against the classical API on the same geometry. The lowering produces the identical discrete problem, so the solutions agree bit-for-bit:\n\nsol_ref = mgb_solve(assemble(amg(geom); p = 1.5); verbose = false)\nmaximum(abs.(value(u) .- sol_ref.z[:, 1]))","title":"Quick tour: the p-Laplacian"},{"category":"section","location":"jump/#Regions:-constraints-on-part-of-the-domain","page":"JuMP","text":"A constraint holds everywhere by default; adding On(pairs) restricts it to a node set given as (vertex, element) pairs — the same format as find_boundary and the low-level dirichlet_nodes API. Equality + On is a Dirichlet condition; inequality/cone + On becomes a piecewise barrier, active only on the region. Region selection is ordinary data preparation — build the pairs with a comprehension.\n\nHere is a membrane pushed upward by a uniform load, with an obstacle imposed only on the left half of the domain:\n\ngeom2 = subdivide(fem2d_P2(), 3)\nVn, Nn = size(geom2.x, 1), size(geom2.x, 2)\nleft = [(v, e) for e in 1:Nn for v in 1:Vn if geom2.x[v, e, 1] < 0]\n\nm2 = MGBModel(geom2)\nset_attribute(m2, \"verbose\", false)\n@variable(m2, u2); @variable(m2, s2, Broken())\nset_start(s2, 100.0)\n@constraint(m2, u2 == Coef(m2, 0.0), On(find_boundary(geom2)))\n@constraint(m2, [deriv(u2, :dx); deriv(u2, :dy); s2] in EpiPower(2.0))\n@constraint(m2, u2 >= Coef(m2, x -> 0.25 - x[1]^2 - x[2]^2), On(left))\n@objective(m2, Min, integral(Coef(m2, -1.0) * u2 + s2))\noptimize!(m2)\nplot(mgb_solution(m2)); savefig(\"jump_obstacle.svg\"); nothing  # hide\nclose()  # hide\n\n(Image: )\n\nThe obstacle binds on its region (the infeasible start is handled by the feasibility phase automatically) and is genuinely absent elsewhere:\n\nphi  = value(Coef(m2, x -> 0.25 - x[1]^2 - x[2]^2))\nlin  = [v + (e - 1) * Vn for (v, e) in left]\nrgt  = setdiff(1:length(phi), lin)\nprintln(\"min(u - φ) on the obstacle region:  \", minimum(value(u2)[lin] .- phi[lin]))\nprintln(\"min(u - φ) off the region:          \", minimum(value(u2)[rgt] .- phi[rgt]))","title":"Regions: constraints on part of the domain"},{"category":"section","location":"jump/#The-Zoo,-restated-in-JuMP","page":"JuMP","text":"Every Zoo problem is a few lines in this syntax; jump/test_zoo.jl checks all six against the classical constructors. Two examples. The minimal surface uses a constant row inside the cone — s ≥ ‖(∇u, 1)‖ is the shifted Lorentz cone:\n\ngu = x -> 0.5 * (x[1]^2 - x[2]^2)\nms = MGBModel(geom)\nset_attribute(ms, \"verbose\", false)\n@variable(ms, v); @variable(ms, sv, Broken())\nset_start(v, gu); set_start(sv, 10.0)\n@constraint(ms, v == Coef(ms, gu), On(find_boundary(geom)))\n@constraint(ms, [deriv(v, :dx); deriv(v, :dy); Coef(ms, 1.0); sv] in EpiPower(1.0))\n@objective(ms, Min, integral(1.0 * sv))\noptimize!(ms)\nref = mgb_solve(Zoo.minimal_surface(amg(geom)); verbose = false)\nmaximum(abs.(value(v) .- ref.z[:, 1]))\n\nRudin–Osher–Fatemi denoising uses spatial data inside a cone — the fidelity slack is r ≥ (u - f_data)²:\n\nfdata = x -> 0.5 * tanh(5 * x[1])\nmr = MGBModel(geom)\nset_attribute(mr, \"verbose\", false)\n@variable(mr, w); @variable(mr, sw, Broken()); @variable(mr, r, Broken())\nset_start(w, fdata); set_start(sw, 10.0); set_start(r, 10.0)\nfd = Coef(mr, fdata)\n@constraint(mr, w == fd, On(find_boundary(geom)))\n@constraint(mr, [deriv(w, :dx); deriv(w, :dy); sw] in EpiPower(1.0))   # s ≥ |∇u|\n@constraint(mr, [w - fd; r] in EpiPower(2.0))                          # r ≥ (u-f)²\n@objective(mr, Min, integral(sw + Coef(mr, 0.5) * r))\noptimize!(mr)\nref = mgb_solve(Zoo.rof(amg(geom)); verbose = false)\nmaximum(abs.(value(w) .- ref.z[:, 1]))","title":"The Zoo, restated in JuMP"},{"category":"section","location":"jump/#What-lowers-to-what","page":"JuMP","text":"model content classical object\ngeometry passed to MGBModel Geometry\nvariables, kinds, Dirichlet constraints state_variables + dirichlet_nodes → amg(geom; dirichlet_nodes)\ndistinct atoms (component, operator) used anywhere the D table\neach cone constraint one Convex piece (convex_linear / convex_Euclidian_power)\nOn regions on cones convex_piecewise selector columns\nintegral(...) objective the cost grid f_grid\nstarts + Dirichlet data the initial/lift grid g_grid\n\nUntagged variables are conforming if differentiated or Dirichlet-constrained and broken otherwise; Broken() / Uniform() override. The model must be in conic form (epigraph slacks are yours to declare, as with any conic solver); pointwise equality requires On; variable bounds and products of variable expressions are rejected with explanatory errors. dual and spectral geometries are not wired up yet.","title":"What lowers to what"},{"category":"section","location":"jump/#Module-reference","page":"JuMP","text":"","title":"Module reference"},{"category":"type","location":"jump/#Main.MultiGridBarrierJuMP.Broken","page":"JuMP","text":"Broken()\n\n@variable tag: the variable lives in the broken space (:full), one degree of freedom per broken node. This is the natural space for epigraph slacks — it makes the pointwise epigraph reformulation exact. Untagged variables are broken by default when they are never differentiated or Dirichlet-constrained.\n\n\n\n\n\n","title":"Main.MultiGridBarrierJuMP.Broken"},{"category":"type","location":"jump/#Main.MultiGridBarrierJuMP.Coef","page":"JuMP","text":"Coef(m::MGBModel, f::Function)\nCoef(m::MGBModel, r::Real)\n\nSpatial data, evaluated eagerly at the broken nodes. f receives the node coordinate as a Vector and must return a Real.\n\n\n\n\n\n","title":"Main.MultiGridBarrierJuMP.Coef"},{"category":"type","location":"jump/#Main.MultiGridBarrierJuMP.EpiPower","page":"JuMP","text":"EpiPower(p)\n\nThe pointwise epigraph set { [q; s] : s ≥ ‖q‖₂^p } with the slack LAST (convex_Euclidian_power's [q; s] convention). p is a Real or a spatial function x -> p(x) ≥ 1.\n\n\n\n\n\n","title":"Main.MultiGridBarrierJuMP.EpiPower"},{"category":"type","location":"jump/#Main.MultiGridBarrierJuMP.MGBModel","page":"JuMP","text":"MGBModel(geom::Geometry) <: JuMP.AbstractModel\n\nA JuMP model over a fixed MultiGridBarrier discretization. Build it from any FEM Geometry (e.g. fem2d_P2(), subdivide(fem2d_P1(), 4)), then use the standard JuMP macros; optimize! lowers the model to amg → assemble → mgb_solve, constructing the AMG hierarchy automatically from the geometry and the Dirichlet constraints. All spatial data (Coef) is evaluated eagerly at the broken nodes at modeling time.\n\nSolver options via set_attribute(m, key, value) with string keys \"prolongator\", \"tol\", \"t\", \"t_feasibility\", \"maxit\", \"kappa\", \"max_newton\", \"verbose\", \"device\".\n\n\n\n\n\n","title":"Main.MultiGridBarrierJuMP.MGBModel"},{"category":"type","location":"jump/#Main.MultiGridBarrierJuMP.On","page":"JuMP","text":"On(pairs::Vector{Tuple{Int,Int}})\n\nConstraint region: a set of broken nodes as (vertex, element) pairs — the same format as find_boundary(geom) and the low-level dirichlet_nodes API.\n\n\n\n\n\n","title":"Main.MultiGridBarrierJuMP.On"},{"category":"type","location":"jump/#Main.MultiGridBarrierJuMP.Uniform","page":"JuMP","text":"Uniform()\n\n@variable tag: the variable is a single global constant (the :uniform subspace).\n\n\n\n\n\n","title":"Main.MultiGridBarrierJuMP.Uniform"},{"category":"method","location":"jump/#JuMP.value-Union{Tuple{Main.MultiGridBarrierJuMP.MGBVarRef{T}}, Tuple{T}} where T","page":"JuMP","text":"value(u)             # values of the component at the broken nodes\nvalue(deriv(u, :dx)) # operator-applied values at the broken nodes\n\nVectors follow the flat broken-node layout (reshape(geom.x, V*N, d) row order). Conforming components have equal values on coincident node copies; broken components genuinely may not.\n\n\n\n\n\n","title":"JuMP.value"},{"category":"method","location":"jump/#Main.MultiGridBarrierJuMP.deriv-Tuple{Main.MultiGridBarrierJuMP.MGBVarRef, Symbol}","page":"JuMP","text":"deriv(u, op::Symbol)\n\nThe atom op applied to variable u, e.g. deriv(u, :dx). op must be a key of geom.operators (:id, :dx, :dy, ...). deriv(u, :id) === u.\n\n\n\n\n\n","title":"Main.MultiGridBarrierJuMP.deriv"},{"category":"method","location":"jump/#Main.MultiGridBarrierJuMP.integral-Tuple{Union{Coef, Main.MultiGridBarrierJuMP.MGBExpr, Main.MultiGridBarrierJuMP.MGBVarRef}}","page":"JuMP","text":"integral(expr)\n\nThe objective functional ∫ expr dx, where expr is affine in the atoms.\n\n\n\n\n\n","title":"Main.MultiGridBarrierJuMP.integral"},{"category":"method","location":"jump/#Main.MultiGridBarrierJuMP.mgb_solution-Tuple{MGBModel}","page":"JuMP","text":"mgb_solution(m::MGBModel) -> MGBSOL\n\nThe underlying MultiGridBarrier solution object (for plot(sol), logs, etc.).\n\n\n\n\n\n","title":"Main.MultiGridBarrierJuMP.mgb_solution"},{"category":"method","location":"jump/#Main.MultiGridBarrierJuMP.set_start-Union{Tuple{T}, Tuple{Main.MultiGridBarrierJuMP.MGBVarRef{T}, Function}} where T","page":"JuMP","text":"set_start(u, f_or_number)\n\nInitial iterate for component u, as a spatial function or a constant. The start value doubles as the Dirichlet lift away from the constrained nodes and should be strictly feasible when possible (e.g. a comfortably large slack).\n\n\n\n\n\n","title":"Main.MultiGridBarrierJuMP.set_start"},{"category":"method","location":"jump/#Main.MultiGridBarrierJuMP.solver_log-Tuple{MGBModel}","page":"JuMP","text":"solver_log(m::MGBModel) -> String\n\n\n\n\n\n","title":"Main.MultiGridBarrierJuMP.solver_log"},{"category":"section","location":"","page":"Home","text":"using Markdown\nusing Pkg\nusing MultiGridBarrier\nv = string(pkgversion(MultiGridBarrier))\nmd\"# MultiGridBarrier $v\"\n\nMultiGridBarrier is a Julia module for solving nonlinear convex optimization problems in function spaces, such as p-Laplace problems. When regularity conditions are satisfied, the solvers are quasi-optimal.\n\nThe package features finite element and spectral discretizations in 1d, 2d, and 3d.\n\nA short software paper describing the package is available as a PDF.","title":"Home"},{"category":"section","location":"#Bibliography","page":"Home","text":"This package implements and builds on a growing line of work on barrier methods for convex problems in function spaces. If you use it in your research, please cite the paper(s) most relevant to your work:\n\nS. Loisel, The Algebraic Multi-Grid-Barrier Method for Solving p-Laplace and Other Convex Optimization Problems, Proceedings of the 29th International Conference on Domain Decomposition Methods, 2026. online PDF\nS. Loisel, The spectral barrier method to solve analytic convex optimization problems in function spaces, Numerische Mathematik 158(1):281–302, 2026. doi:10.1007/s00211-025-01508-0\nS. Loisel, Efficient algorithms for solving the p-Laplacian in polynomial time, Numerische Mathematik 146(2):369–400, 2020. doi:10.1007/s00211-020-01141-z","title":"Bibliography"},{"category":"section","location":"#Workflow-at-a-glance","page":"Home","text":"Every problem is solved with the same four-step pattern:\n\nBuild a single-level Geometry with one of the mesh constructors (fem1d, fem2d, fem2d_P1, fem2d_P2, fem3d, spectral1d, spectral2d). These take only mesh inputs — no L, no hierarchy.\nAttach a multigrid hierarchy with amg(geom) (algebraic multigrid). Returns a MultiGrid.\nAssemble the problem with assemble(mg; kwargs...), returning an MGBProblem.\nSolve with mgb_solve(prob; kwargs...).\n\nTo solve on a GPU, load the CUDA extension (using CUDA, CUDSS_jll) and pass device = CUDADevice to mgb_solve; the assembled problem is moved to the device, solved there, and the returned MGBSOL is moved back to native CPU types. With a functional GPU present this becomes the default device — pass device = CPUDevice to force the CPU.\n\nIf you want a finer mesh than the single-level geom provides, refine it first with subdivide(geom, L) and then attach AMG: amg(subdivide(geom, L)). The legacy geometric_mg(geom, L) builds a geometric-subdivision hierarchy instead of AMG; it is still available for callers that specifically want geometric transfers.","title":"Workflow at a glance"},{"category":"section","location":"#Choosing-the-AMG-coarsening","page":"Home","text":"For the FEM discretizations, amg(geom; prolongator=...) selects how the multigrid hierarchy is built. A prolongator is a callable mapping a stiffness matrix to its level prolongations; three factories are provided (each forwards keyword arguments such as max_coarse to the underlying builder):\n\namg_ruge_stuben(; kwargs...) — classical Ruge–Stüben (the default), via AlgebraicMultigrid.jl.\namg_smoothed_aggregation(; kwargs...) — smoothed aggregation, via the same package.\namg_pyamg(; solver=:rootnode, kwargs...) — the Python pyamg package (:rootnode energy-minimization, :smoothed_aggregation, or :ruge_stuben), imported lazily through PyCall.\n\nmgb_solve(assemble(amg(fem2d_P1(); prolongator = amg_ruge_stuben(max_coarse=4)); p=1.5); verbose=false);","title":"Choosing the AMG coarsening"},{"category":"section","location":"#Finite-elements","page":"Home","text":"A 1d p-Laplace problem:\n\nusing PyPlot # hide\nusing MultiGridBarrier\nnodes = collect(range(-1.0, 1.0, length=33))\ngeom = fem1d(; nodes)\nplot(mgb_solve(assemble(amg(geom); p=1.0); verbose=false));\nsavefig(\"fem1d.svg\"); nothing # hide\nclose() #hide\n\n(Image: )\n\nA 2d p-Laplace problem on tensor-product Q_k quadrilaterals (here biquadratic, k=2):\n\nplot(mgb_solve(assemble(amg(subdivide(fem2d(; k=2), 3)); p=1.0); verbose=false));\nsavefig(\"fem2d.svg\"); nothing # hide\nclose() #hide\n\n(Image: )\n\nThe same problem on P2+cubic-bubble triangles (fem2d_P2):\n\nplot(mgb_solve(assemble(amg(subdivide(fem2d_P2(), 3)); p=1.0); verbose=false));\nsavefig(\"fem2d_P2.svg\"); nothing # hide\nclose() #hide\n\n(Image: )","title":"Finite elements"},{"category":"section","location":"#Spectral-elements","page":"Home","text":"1d p-Laplace via spectral elements:\n\nplot(mgb_solve(assemble(amg(spectral1d(n=40)); p=1.0); verbose=false));\nsavefig(\"spectral1d.svg\"); nothing # hide\nclose() #hide\n\n(Image: )\n\nA 2d p-Laplace problem:\n\nplot(mgb_solve(assemble(amg(spectral2d(n=5)); p=1.5); verbose=false));\nsavefig(\"spectral2d.svg\"); nothing # hide\nclose() #hide\n\n(Image: )","title":"Spectral elements"},{"category":"section","location":"#Solving-\\infty-Laplacians","page":"Home","text":"For p geq 1 on a domain Omega, the p-Laplace problem minimises J(u) = nabla u_L^p(Omega)^p + int_Omega fu where u lies in a suitable function space satisfying, e.g., Dirichlet conditions and f is a forcing. The infty-Laplace problem replaces the inner norm by L^infty: J(u) = nabla u_L^infty(Omega)^p + int_Omega fu We take p=1 below for simplicity.\n\nplot(mgb_solve(assemble(amg(fem1d(; nodes)); p=1.0, state_variables=[:u :dirichlet; :s :uniform]); verbose=false));\nsavefig(\"fem1dinfty.svg\"); nothing # hide\nclose() #hide\n\n(Image: )","title":"Solving infty-Laplacians"},{"category":"section","location":"#Parabolic-problems","page":"Home","text":"A time-dependent problem:\n\nplot(parabolic_solve(amg(subdivide(fem2d_P1(), 3)); h=0.1, verbose=false))","title":"Parabolic problems"},{"category":"section","location":"#3D-Finite-Elements","page":"Home","text":"fem3d provides 3D hexahedral Q_k finite elements — the 3D case of the same dimension-generic tensor-product family as fem1d/fem2d — visualized with PyVista.\n\nsol = mgb_solve(assemble(amg(subdivide(fem3d(; k=1), 2))); verbose=false)\nfig = plot(sol)\nsavefig(fig, \"fem3d_demo.png\"); nothing # hide\n\n(Image: )\n\nA time-dependent 3D problem:\n\nplot(parabolic_solve(amg(subdivide(fem3d(; k=1), 2)); h=0.1, verbose=false))","title":"3D Finite Elements"},{"category":"section","location":"#Embedded-manifolds","page":"Home","text":"fem1d and fem2d also build embedded manifolds — a curve or surface living in a higher-dimensional ambient space — when you pass ambient=Val(e) together with a mesh K whose third dimension has e ≥ d coordinate columns. The cases are a curve in ℝ² or ℝ³ (fem1d, e = 2 or 3) and a surface in ℝ³ (fem2d, e = 3); the discretization type carries both dimensions as TensorFEM{d,e,T} (e.g. TensorFEM{2,3,Float64} is a surface in ℝ³).\n\nOn an embedded manifold the operators :dx, :dy[, :dz] are the e ambient components of the intrinsic (tangential) gradient ∇Γ — each output vector is tangent to the manifold, so `n · ∇Γ u = 0holds by construction and no normal vector is ever needed (which keeps higher codimension well-defined). The quadrature weightsgeom.ware the induced surface / arc-length measure√det(JᵀJ). Closed manifolds (circles, spheres, tori) close up automatically when coincident element corners are deduplicated; passt` explicitly for a glued/periodic mesh.\n\nEverything downstream is unchanged: amg(geom), assemble, and mgb_solve work as usual. The only adjustment when posing a problem is that the operator matrix D must list all e ambient gradient components (e.g. :dx :dy :dz for a surface), since default_D keys on the intrinsic dimension; see test/test_manifold.jl for a worked scalar p-Laplace-plus-mass solve on a circle and a sphere.\n\nPlotting follows one rule — graph the solution into the first free dimension, else show it as color: a curve in ℝ² is drawn as the height-graph (x, y, u), a curve in ℝ³ as a tube colored by u, and a surface in ℝ³ as a colored surface. For example, cos 2θ on the unit circle:\n\nN = 120\nθ = range(0, 2π, length=N+1)\nK = zeros(2, N, 2)\nfor e in 1:N\n    K[1, e, :] = [cos(θ[e]),   sin(θ[e])]\n    K[2, e, :] = [cos(θ[e+1]), sin(θ[e+1])]\nend\ncircle = fem1d(; K = K, ambient = Val(2))          # a curve in ℝ²: TensorFEM{1,2}\nu = [cos(2 * atan(circle.x[v, e, 2], circle.x[v, e, 1]))\n     for e in 1:size(circle.x, 2) for v in 1:size(circle.x, 1)]\nfig = plot(circle, u)\nsavefig(fig, \"manifold_circle.png\"); nothing # hide\n\n(Image: )","title":"Embedded manifolds"},{"category":"section","location":"#Front-end-summary","page":"Home","text":"The mesh constructors below all return a single-level Geometry. Boundary conditions are chosen later, at amg time, via its dirichlet_nodes keyword — the default constrains the whole boundary (homogeneous-trace Dirichlet); pass subsets or named node sets for mixed and per-component conditions.\n\nFunction Element Dim Key kwargs\nfem1d Q_k interval (P1 at k=1) 1D (curve in 2D/3D) nodes (or K), k, ambient (defaulted)\nfem2d Q_k quadrilaterals 2D (surface in 3D) K, k, ambient (defaulted)\nfem2d_P1 P1 triangles 2D K (defaulted)\nfem2d_P2 P2 + cubic bubble triangles 2D K (defaulted)\nfem3d Q_k hexahedra 3D K, k (defaulted)\nspectral1d spectral (Chebyshev) 1D n (defaulted)\nspectral2d spectral (tensor Chebyshev) 2D n (defaulted)\n\nfem1d/fem2d/fem3d are the tensor-product Q_k family (the map is isoparametric, so curved elements are supported); fem2d_P1/fem2d_P2 are the simplicial P_k family on triangles.\n\nEvery constructor except fem1d has a default mesh, and every other keyword is defaulted (including n for the spectral fronts); fem1d needs its element endpoints nodes — or the mesh tensor K passed directly. A mesh is fundamentally a connectivity/coordinates pair (t, x): you give the coordinates as K, and the tensor-product constructors fem1d/fem2d/fem3d take the connectivity as an optional t= keyword (deduced from the coordinates when omitted) — supply it for slit domains and glued manifolds where coincident nodes must stay distinct. See Meshes, coordinates, and connectivity.\n\nPass the resulting Geometry to amg(geom) to obtain a MultiGrid — an algebraic- multigrid hierarchy on the fine mesh. To refine the mesh first, compose with subdivide(geom, L): amg(subdivide(geom, L)).\n\nThe legacy geometric_mg(geom, L) builds a geometric-subdivision hierarchy instead of AMG; it remains available for callers that specifically want geometric transfers.\n\nPrefer stating problems in an algebraic modeling language? The JuMP front end accepts standard @variable/@constraint/@objective syntax and lowers it to this same pipeline, building the hierarchy automatically.","title":"Front-end summary"},{"category":"section","location":"#Meshes,-coordinates,-and-connectivity","page":"Home","text":"Fundamentally a mesh — and the Geometry that holds it — is a pair (t, x):\n\nt, the connectivity (geom.t): an integer matrix of shape (V, N); t[v,e] is the global id of local node v in element e. It records which per-element nodes are the same global node, so maximum(geom.t) is the number of distinct nodes. The AMG hierarchy and find_boundary consult geom.t.\nx, the coordinates (geom.x): a 3-tensor of shape (V, N, D) in the broken / discontinuous-Galerkin layout — each element carries its own copy of every local node — giving the node positions (the geometry, possibly curved/isoparametric).\n\nKeeping the two apart is the point: coincident nodes can be glued (one shared t id) or kept topologically distinct (different ids at the same coordinates). The latter — which no coordinate-only description can express — is how slit domains, branch cuts, and glued manifolds (e.g. the Riemann surface of √z) are represented. You pass the coordinates to the tensor-product constructors as the K keyword (it becomes geom.x) and the connectivity as the t keyword:\n\ngeom = fem2d(k = 2, K = coords, t = connectivity)\n\nConvenience — omit t. For an ordinary embedded mesh you rarely have connectivity in hand, and you don't need it: leave t off and it is recovered automatically by deduplicating coincident coordinates (two per-element nodes at the same point become one global DOF). This is the everyday path — every example above uses it:\n\ngeom = fem2d(k = 2, K = coords)   # t deduced from coincident coordinates","title":"Meshes, coordinates, and connectivity"},{"category":"section","location":"#Coordinates:-the-K-tensor-in-detail","page":"Home","text":"All FEM constructors take their coordinates as a K keyword argument (fem1d(; nodes) derives K from nodes by default). K is a 3-tensor K::Array{T,3} of shape (V, N, D):\n\nV is the number of local nodes per element. For the tensor-product Q_k family it is (k+1)^d: k+1 for fem1d, (k+1)^2 for fem2d, (k+1)^3 for fem3d (you may instead pass the 2^d-corner tensor — 2, 4 or 8 — for straight elements, which is promoted internally). For the simplicial family V is 3 for fem2d_P1 and 7 for fem2d_P2;\nN is the number of elements;\nD is the ambient (embedding) dimension — the number of coordinate components, 1, 2, or 3. For an ordinary codimension-0 mesh it equals the intrinsic element dimension d (1 for fem1d, 2 for fem2d, 3 for fem3d); for an embedded manifold (a curve or surface living in a higher-dimensional space) it exceeds d — see Embedded manifolds below.\n\nK[v, e, d] is the d-th coordinate of the v-th local node of the e-th element. So in 1D, nodes x[1] < x[2] < … < x[m] defining elements [x[1],x[2]], [x[2],x[3]], …, [x[m-1],x[m]] give a (2, m-1, 1) tensor\n\nK = reshape(T[x[1], x[2], x[2], x[3], x[3], x[4], …, x[m-1], x[m]], 2, m-1, 1)\n\nwith each interior node appearing twice (once per neighbouring element). The stored geom.x carries the same shape as K; the flat (V·N, D) view used by sparse operators is recovered via reshape(geom.x, :, size(geom.x, 3)).\n\nSpectral discretizations (spectral1d, spectral2d) have no element structure and use N = 1 — geom.x has shape (n, 1, 1) in 1D and (n², 1, 2) in 2D.","title":"Coordinates: the K tensor in detail"},{"category":"section","location":"#Building-t-for-high-order:-tensor_dofmap","page":"Home","text":"A mesh generator gives you corner connectivity (2^d × N), but a Q_k mesh also needs a global id for every edge/face/interior node. tensor_dofmap builds the full (k+1)^d × N connectivity from corner connectivity t_corner alone — no coordinates — numbering shared edges/faces consistently (with edge orientation at k≥3), so any slit/branch-cut structure in the corner connectivity propagates to the high-order nodes. It supports any d for k≤2 and any k for d≤2; it throws on shared 3D face-interior grids (d≥3, k≥3), where you supply t by hand or use the dedup default.\n\nFor example, two Q2 quads sharing the seam x = 1:\n\n# coordinates (corner shorthand): [0,1]² and [1,2]×[0,1]\nK = reshape(Float64[0 0; 1 0; 0 1; 1 1;        # element 1 corners\n                    1 0; 2 0; 1 1; 2 1],       # element 2 corners\n            4, 2, 2)\n\nglued = fem2d(k = 2, K = K, t = tensor_dofmap([1 2; 2 5; 3 4; 4 6], 2, Val(2)))  # 15 nodes\nslit  = fem2d(k = 2, K = K, t = tensor_dofmap([1 7; 2 5; 3 8; 4 6], 2, Val(2)))  # 18 nodes\n\nThe coordinates are identical; only the connectivity differs. In glued the seam is a shared interior edge (15 distinct nodes); in slit element 2's left corners carry distinct ids 7, 8, so the seam splits into two boundaries (18 distinct nodes) — a result the coordinate-dedup default cannot produce.","title":"Building t for high order: tensor_dofmap"},{"category":"section","location":"#amg-vs-subdivide-vs-geometric_mg","page":"Home","text":"amg(geom) is the recommended hierarchy: it builds AMG from the fine mesh's continuous-P1 stiffness. Works whether geom came from your own mesher or from uniform subdivision via subdivide.\nsubdivide(geom, L) is mesh refinement only: it returns a fine Geometry obtained by subdividing each input element L−1 times. Compose with amg(subdivide(geom, L)) to get AMG on the refined mesh.\ngeometric_mg(geom, L) is the legacy alternative: it produces a hierarchy of L levels using purely geometric subdivision transfers (no AMG coarsening). Kept for cases that need geometric transfers specifically.","title":"amg vs subdivide vs geometric_mg"},{"category":"section","location":"#Module-reference","page":"Home","text":"","title":"Module reference"},{"category":"section","location":"#Types-reference","page":"Home","text":"","title":"Types reference"},{"category":"section","location":"#Functions-reference","page":"Home","text":"","title":"Functions reference"},{"category":"section","location":"#Index","page":"Home","text":"","title":"Index"},{"category":"module","location":"#MultiGridBarrier.MultiGridBarrier","page":"Home","text":"module MultiGridBarrier\n\nMultiGridBarrier solves nonlinear convex optimization problems in function spaces using a barrier (interior-point) method accelerated by a multigrid hierarchy. The package exposes:\n\nSingle-level mesh constructors: fem1d, fem2d, fem2d_P1, fem2d_P2, fem3d, spectral1d, spectral2d. Each returns a Geometry. fem1d/fem2d also build embedded manifolds — a curve or surface in a higher ambient dimension — via ambient=Val(e) (intrinsic gradient operators, surface/arc-length measure); see the manual's \"Embedded manifolds\".\nTopological connectivity for slit domains / glued manifolds: tensor_dofmap builds full-node connectivity from corner connectivity (no coordinates); pass the result as the t= keyword of fem1d/fem2d/fem3d so geometrically-coincident nodes stay distinct.\nThe hierarchy builder: amg(geom) wraps a Geometry and returns a MultiGrid (algebraic-multigrid hierarchy on the fine mesh).\nA mesh-refinement utility: subdivide(geom, L) returns a refined Geometry. Compose with AMG via amg(subdivide(geom, L)).\nA legacy geometric-subdivision hierarchy: geometric_mg(geom, L). Still available for callers that specifically want geometric transfers; new code should prefer amg.\nProblem assembly: assemble(mg::MultiGrid; kwargs...) -> MGBProblem.\nThe main solver: mgb_solve(prob::MGBProblem; kwargs...) -> MGBSOL.\nA time-dependent solver: parabolic_solve(mg::MultiGrid; kwargs...).\n\nA gentle introduction via the p-Laplacian\n\nFor a domain Ω ⊂ ℝᵈ and p ≥ 1, consider the variational problem\n\nmin_u  J(u) = int_Omega nabla u_2^p + fu  dx\n\nsubject to appropriate boundary conditions (e.g., homogeneous Dirichlet). The Euler–Lagrange equation gives the p-Laplace PDE.\n\nTypical workflow\n\ngeom = fem2d_P2()                       # single-level mesh\nmg   = amg(geom)                        # build AMG hierarchy\nprob = assemble(mg; p = 1.5)            # assemble the (native) MGBProblem\nsol  = mgb_solve(prob)\nplot(sol)\n\nCUDA GPU acceleration (optional extension)\n\nCUDA support is provided as a Julia package extension (MultiGridBarrierCUDAExt). Load CUDA and CUDSS_jll to enable it, then select the GPU with device = CUDADevice:\n\nusing CUDA, CUDSS_jll\nusing MultiGridBarrier\nsol = mgb_solve(assemble(amg(fem2d_P2()); p = 1.5); device = CUDADevice)\n\nmgb_solve moves the assembled problem to the GPU, solves there, and moves the solution back, so the returned MGBSOL is always in native CPU types. When a functional GPU is present the default device becomes CUDADevice; pass device = CPUDevice to force the CPU. The lower-level native_to_cuda / cuda_to_native converters remain available.\n\nSee also\n\nMesh constructors: fem1d, fem2d, fem2d_P1, fem2d_P2, fem3d, spectral1d, spectral2d; tensor_dofmap (+ the t= keyword) for slit-domain / glued-manifold connectivity.\nHierarchy: amg, subdivide (and the legacy geometric_mg).\nSolvers: mgb_solve, parabolic_solve.\nConvex: convex_Euclidian_power, convex_linear, convex_piecewise, intersect.\nVisualization & sampling: plot, interpolate.\nBackend selection: device kwarg of mgb_solve, CPUDevice, CUDADevice, native_to_device, device_to_native, default_device.\nProblem assembly: assemble, MGBProblem.\nCUDA (lower level): native_to_cuda, cuda_to_native, clear_cudss_cache!.\n\n\n\n\n\n","title":"MultiGridBarrier.MultiGridBarrier"},{"category":"type","location":"#MultiGridBarrier.CPUDevice","page":"Home","text":"CPUDevice <: Device\n\nThe native CPU backend; native_to_device/device_to_native are the identity.\n\n\n\n\n\n","title":"MultiGridBarrier.CPUDevice"},{"category":"type","location":"#MultiGridBarrier.CUDADevice","page":"Home","text":"CUDADevice <: Device\n\nThe CUDA GPU backend. Requires the MultiGridBarrierCUDAExt extension, enabled by using CUDA, CUDSS_jll before (or alongside) using MultiGridBarrier.\n\n\n\n\n\n","title":"MultiGridBarrier.CUDADevice"},{"category":"type","location":"#MultiGridBarrier.Convex","page":"Home","text":"Convex{T}\n\nContainer for a convex constraint set used by the MGB solver.\n\nFields:\n\nbarrier: (F0, F1, F2) - value, gradient, Hessian functions\ncobarrier: (F0, F1, F2) - with slack for feasibility\nslack: initial slack function\nargs: tuple of parameter arrays, splatted to maprowsgpu\n\nBarrier functions receive (args_rows..., y) where args_rows are per-vertex parameter data (via broadcasting), and y is the solution SVector. This enables true GPU execution without scalar indexing.\n\nConstruct via helpers like convex_linear, convex_Euclidian_power, convex_piecewise, or intersect. These helpers return a single Convex{T} (the barrier is evaluated only at the fine level).\n\n\n\n\n\n","title":"MultiGridBarrier.Convex"},{"category":"type","location":"#MultiGridBarrier.Device","page":"Home","text":"abstract type Device\n\nMarker selecting the compute backend for mgb_solve. Concrete devices are CPUDevice (always available) and CUDADevice (requires the CUDA extension: using CUDA, CUDSS_jll).\n\n\n\n\n\n","title":"MultiGridBarrier.Device"},{"category":"type","location":"#MultiGridBarrier.FEM2D_P1","page":"Home","text":"FEM2D_P1{T}\n\nDiscretization tag for 2D P1 triangular FEM. Stores the user's per-element corner tensor K of shape (3, N, 2) — K[v, t, d] is coordinate d of vertex v of triangle t.\n\n\n\n\n\n","title":"MultiGridBarrier.FEM2D_P1"},{"category":"type","location":"#MultiGridBarrier.FEM2D_P2","page":"Home","text":"FEM2D_P2{T}\n\n2D FEM (P2+bubble) discretization descriptor. Stores the corner triangulation K shape (3, N, 2) and the canonical P2+bubble mesh K7 shape (7, N, 2).\n\n\n\n\n\n","title":"MultiGridBarrier.FEM2D_P2"},{"category":"type","location":"#MultiGridBarrier.Geometry","page":"Home","text":"Geometry{T,X<:AbstractArray{T,3},W,M_op,Discretization}\n\nSingle-level container for discretization geometry. Holds only the fine-level mesh and operators — no multigrid hierarchy. Use amg(geom) to attach an algebraic-multigrid hierarchy and return a MultiGrid. (The legacy geometric_mg(geom, L) builds a geometric-subdivision hierarchy instead; new code should prefer amg.)\n\nType parameters\n\nT: scalar numeric type (e.g. Float64)\nX<:AbstractArray{T,3}: type of the mesh tensor x (typically Array{T,3}).\nW: type of the weight storage w (typically Vector{T}).\nM_op: matrix type for operators (e.g. SparseMatrixCSC{T,Int}, BlockDiag{T}).\nDiscretization: front-end descriptor (e.g. FEM1D{e,T}, FEM2D_P2{T}, SPECTRAL1D{T}).\n\nFields\n\ndiscretization::Discretization: discretization descriptor encoding dimension and grid info.\nt::Matrix{Int}: cached element connectivity of shape (V, N) — t[v, e] is the global node id of local node v in element e, so broken nodes that coincide across elements share an id and maximum(t) is the number of distinct nodes. The 4-arg constructor derives t from x by geometric dedup; the inner 5-arg constructor (and fem1d/fem2d/fem3d with t=) take it verbatim, letting coincident-but-distinct nodes (slits, branch cuts, glued manifolds) stay topologically separate. The amg path (find_boundary, amg) reads t directly; coordinate dedup builds t only in the 4-arg constructor and the legacy geometric_mg path. Always a CPU Matrix{Int}, even for GPU geometries.\nx::X: mesh as a 3-tensor of shape (V, N, D) — V vertices per element, N elements, D spatial dimensions. Reshape-compatible with the legacy flat layout via reshape(x, V*N, D) (zero-copy). Spectral discretizations use N = 1 (a single notional \"element\" comprising every Chebyshev node).\nw::W: quadrature weights matching the flattened node order (length V*N).\noperators::Dict{Symbol,M_op}: fine-level discrete operators (e.g. :id, :dx, :dy).\n\n\n\n\n\n","title":"MultiGridBarrier.Geometry"},{"category":"type","location":"#MultiGridBarrier.HTML5anim","page":"Home","text":"HTML5anim\n\nWrapper around an HTML5 <video> produced by the parabolic-animation plot methods. It carries the rendered video markup in its anim::String field and defines show(io, MIME\"text/html\"(), ::HTML5anim), so returning one as the last value of a Jupyter/Pluto cell (or in Documenter output) embeds the animation inline.\n\n\n\n\n\n","title":"MultiGridBarrier.HTML5anim"},{"category":"type","location":"#MultiGridBarrier.MGB3DFigure","page":"Home","text":"MGB3DFigure\n\nA rendered 3D plot returned by the FEM3D plot methods; the png field holds the PNG bytes. Displays inline as image/png (Jupyter, Documenter, …); write it to a file with savefig(fig, \"out.png\").\n\n\n\n\n\n","title":"MultiGridBarrier.MGB3DFigure"},{"category":"type","location":"#MultiGridBarrier.MGBConvergenceFailure","page":"Home","text":"MGBConvergenceFailure <: Exception\n\nThrown when the MGB solver fails to converge (feasibility or main phase). Includes a descriptive message about the failure.\n\n\n\n\n\n","title":"MultiGridBarrier.MGBConvergenceFailure"},{"category":"type","location":"#MultiGridBarrier.MGBProblem","page":"Home","text":"MGBProblem{T,MT,FT,GT,QT,GeoT}\n\nA fully assembled, array-valued, closure-free convex problem ready for the MGB solver. Produced by assemble; consumed by mgb_solve. Every closure that specifies the problem (f, g, and the constraint data A, b, p, select) has been lowered to per-vertex grids during assembly, so an MGBProblem is pure data: moving it between backends (native_to_device) is plain array movement, and a MGBProblem{T,...,CuArray,...} differs from its CPU sibling only by array type.\n\nFields\n\nM: the (main, feasibility) AMG hierarchy pair (_prepare_amg output).\nf: linear-term grid (f_grid).\ng: Dirichlet/initial-data grid (g_grid).\nQ::Convex: the convex constraint (its args are per-vertex grids).\ngeometry: the fine-level Geometry (for the returned solution / plotting).\n\n\n\n\n\n","title":"MultiGridBarrier.MGBProblem"},{"category":"type","location":"#MultiGridBarrier.MultiGrid","page":"Home","text":"MultiGrid{T,M_R,G<:Geometry{T}}\n\nA Geometry plus a multigrid hierarchy. For each state-variable subspace symbol (:dirichlet, :full, :uniform, or a user-named Dirichlet class) the hierarchy stores a single family of fine-level prolongations R[X][l]: the matrix that lifts level-l-X-subspace coefficients directly to the fine broken basis (the R_{ℓ,X} of the paper). R[X][end] is the fine-level subspace embedding itself (identity for :full, the constant column for :uniform, the continuous zero-trace embedding for Dirichlet classes).\n\nThe per-level (level → level+1) transfer/restriction operators are an implementation detail of amg/geometric_mg: they are composed into the R[X][l] at construction time and not retained, since the solver only ever evaluates the barrier at the fine level and restricts to a coarse search space via R[X][l].\n\nFields\n\ngeometry::G: the fine-level Geometry.\nR::Dict{Symbol,Vector{M_R}}: R[X][l] is the level-l → fine prolongation in subspace X.\n\nConstructors\n\nMultiGrid(geometry, subspaces, refine) — stretches the per-subspace hierarchies to a common depth and composes R[X][l] = (refine[X] chain l→L) · subspaces[X][l]. Accepts either per-subspace Dict transfers or plain Vectors (replicated across every subspace key).\nMultiGrid(geometry, R) — store precomposed prolongations directly (e.g. the Kronecker construction in spectral2d).\n\n\n\n\n\n","title":"MultiGridBarrier.MultiGrid"},{"category":"type","location":"#MultiGridBarrier.SPECTRAL1D","page":"Home","text":"SPECTRAL1D{T}\n\n1D spectral discretization descriptor (Chebyshev). Field: n::Int (nodes).\n\n\n\n\n\n","title":"MultiGridBarrier.SPECTRAL1D"},{"category":"type","location":"#MultiGridBarrier.SPECTRAL2D","page":"Home","text":"SPECTRAL2D{T}\n\n2D spectral discretization descriptor (tensor Chebyshev). Field: n::Int (nodes per dim).\n\n\n\n\n\n","title":"MultiGridBarrier.SPECTRAL2D"},{"category":"type","location":"#MultiGridBarrier.TensorFEM","page":"Home","text":"TensorFEM{d, e, T}\n\nDiscretization descriptor for a d-dimensional tensor-product Q_k Lagrange FEM embedded in ambient dimension e ≥ d (an embedded manifold when e > d). Here d is the intrinsic dimension (number of reference axes) and e the ambient / embedding dimension (= size(geom.x, 3)); both are integers, T the scalar type.\n\nFields\n\nk::Int: polynomial order (each element carries (k+1)^d Lagrange-Chebyshev nodes).\nK::Array{T,3}: the Q1 corner mesh tensor of shape (2^d, N, e) — K[v, j, c] is ambient coordinate c of corner v of element j. Corners are in tensor-product order over {-1,+1}^d (axis-1 fastest). Informational — it records the corner input; the hierarchy builders work from the stored full node tensor geom.x.\n\nFEM1D = TensorFEM{1}, FEM2D = TensorFEM{2}, FEM3D = TensorFEM{3} are aliases fixing only the intrinsic dimension (leaving e, T free): e.g. TensorFEM{2,2} is a planar quad mesh, TensorFEM{2,3} a surface in ℝ³, TensorFEM{1,3} a curve in ℝ³.\n\n\n\n\n\n","title":"MultiGridBarrier.TensorFEM"},{"category":"method","location":"#Base.intersect-Union{Tuple{T}, Tuple{MultiGrid, Convex{T}, Vararg{Convex{T}}}} where T","page":"Home","text":"intersect(mg::MultiGrid, U::Convex{T}, rest...) where {T}\n\nReturn the intersection of convex domains as a single Convex{T}. Equivalent to convex_piecewise with all pieces active at all vertices.\n\n\n\n\n\n","title":"Base.intersect"},{"category":"function","location":"#MultiGridBarrier.amg","page":"Home","text":"amg(geom::Geometry; prolongator=amg_ruge_stuben(max_coarse=2), dirichlet_nodes=Dict(:dirichlet => find_boundary(geom))) -> MultiGrid   # FEM\namg(geom::Geometry) -> MultiGrid                                                                          # spectral\n\nBuild an algebraic-multigrid hierarchy on top of geom, returning a MultiGrid. Dispatched per discretization; the hierarchy's fine level matches geom.\n\nKeyword arguments (FEM discretizations: FEM1D, FEM2D_P1, FEM2D_P2, FEM3D)\n\nprolongator = amg_ruge_stuben(max_coarse=2): the algebraic-multigrid hierarchy builder used to coarsen the auxiliary P1 problem. A prolongator is a callable mapping a SparseMatrixCSC{Float64,Int} stiffness to the vector of level prolongation matrices (finest → coarsest); these set the depth of the hierarchy. Three factory functions construct one while capturing the underlying AMG parameters:\namg_ruge_stuben(; kwargs...) — classical Ruge–Stüben (the default), via AlgebraicMultigrid.ruge_stuben. The depth parameter max_coarse is set through the factory, e.g. amg(geom; prolongator=amg_ruge_stuben(max_coarse=4)).\namg_smoothed_aggregation(; kwargs...) — smoothed aggregation, via AlgebraicMultigrid.smoothed_aggregation.\namg_pyamg(; solver=:rootnode, kwargs...) — the Python pyamg package (:rootnode energy-minimization, :smoothed_aggregation, or :ruge_stuben), imported lazily through PyCall.\nAll kwargs are forwarded to the underlying solver.\ndirichlet_nodes::Dict{Symbol,Vector{Tuple{Int,Int}}} = Dict(:dirichlet => find_boundary(geom)): one entry per zero-trace (\"dirichlet-style\") subspace. Each key is a subspace symbol you may assign to a state-variable component via state_variables, and its value is the set of mesh nodes constrained to zero trace in that subspace, given as (vertex, element) index pairs (the format find_boundary returns).\nThe default builds a single :dirichlet subspace constraining the whole boundary ∂Ω. To impose different Dirichlet boundaries on different components, name them and supply a node set for each, e.g. for z = (u, s) with u clamped on the north edge and s on the south:\nstate_variables = [:u :dirichlet_north\n                   :s :dirichlet_south]\nmg = amg(geom; dirichlet_nodes = Dict(:dirichlet_north => north_pairs,\n                                      :dirichlet_south => south_pairs))\nPer entry, pass a subset of ∂Ω for mixed Dirichlet/Neumann conditions, Tuple{Int,Int}[] for pure Neumann, or a single pinned node to break the constant nullspace. The reserved subspaces :full (all-corners Neumann) and :uniform (global constants) are always available and must not appear as keys. These select which nodes are constrained; the boundary values (the Dirichlet lift g) are supplied separately to mgb_solve.\nauxiliary_postprocess::Function = identity  (opt-in; the tensor-product family FEM1D/FEM2D/FEM3D, and FEM2D_P1 — not FEM2D_P2): a unary function applied to the all-corners (Neumann) auxiliary stiffness before it is fed to prolongator. Use to swap the geometric Galerkin matrix for a graph-Laplacian-style operator that AMG coarsens on graph topology alone. Example: pass the combinatorial Laplacian of the same sparsity (off-diag = -1, diag = degree) when running aggregation-based prolongators (amg_smoothed_aggregation, amg_pyamg(:rootnode)) on highly anisotropic problems near p=1 — that regime can otherwise blow up Newton iteration counts on the central path. The default identity is robust across the bench (the geometric stiffness encodes useful per-row scaling that RS uses), so this is an opt-in escape hatch rather than a recommended default.\n\nSpectral discretizations (SPECTRAL1D, SPECTRAL2D)\n\namg(geom) takes no keyword arguments. The zero-trace subspace is built by basis truncation rather than node masking, so dirichlet_nodes does not apply.\n\nSee also find_boundary, geometric_mg, subdivide.\n\n\n\n\n\n","title":"MultiGridBarrier.amg"},{"category":"method","location":"#MultiGridBarrier.amg_pyamg-Tuple{}","page":"Home","text":"amg_pyamg(; solver::Symbol=:rootnode, kwargs...) -> prolongator\n\nBuild a prolongator backed by the Python pyamg package (imported lazily via PyCall, installing from conda-forge if necessary). solver selects the pyamg solver: :rootnode (rootnode energy-minimization, the default), :smoothed_aggregation, or :ruge_stuben. Any kwargs are forwarded to the pyamg solver constructor. Pass the result to amg(geom; prolongator=...).\n\n\n\n\n\n","title":"MultiGridBarrier.amg_pyamg"},{"category":"method","location":"#MultiGridBarrier.amg_ruge_stuben-Tuple{}","page":"Home","text":"amg_ruge_stuben(; kwargs...) -> prolongator\n\nBuild a prolongator that runs classical Ruge–Stüben AMG via AlgebraicMultigrid.ruge_stuben (the package default). Any kwargs (e.g. max_coarse=4) are forwarded. Pass the result to amg(geom; prolongator=...).\n\n\n\n\n\n","title":"MultiGridBarrier.amg_ruge_stuben"},{"category":"method","location":"#MultiGridBarrier.amg_smoothed_aggregation-Tuple{}","page":"Home","text":"amg_smoothed_aggregation(; kwargs...) -> prolongator\n\nBuild a prolongator that runs smoothed-aggregation AMG via AlgebraicMultigrid.smoothed_aggregation. Any kwargs (e.g. max_coarse=4) are forwarded. Pass the result to amg(geom; prolongator=...).\n\n\n\n\n\n","title":"MultiGridBarrier.amg_smoothed_aggregation"},{"category":"method","location":"#MultiGridBarrier.assemble-Union{Tuple{MultiGrid{T, M_R, G} where {M_R, G<:(Geometry{T, X} where X<:AbstractArray{T, 3})}}, Tuple{T}} where T","page":"Home","text":"assemble(mg::MultiGrid{T}; kwargs...) -> MGBProblem\n\nLower a problem specification to a closure-free, CPU MGBProblem. This is the single place where the problem closures are evaluated to grids: f/g via map_rows, and the constraint closures inside Q at its construction. The result is a backend-agnostic, native data structure; to solve on a GPU, hand it to mgb_solve(prob; device=CUDADevice), which moves it to the device and the solution back.\n\nThe five MGBProblem fields are built from mg and the keyword arguments as:\n\nfield value\nM _prepare_amg(mg; state_variables, D) — the (main, feasibility) AMG pair\nf f_grid, default map_rows(f, x) — the linear-term closure sampled at x\ng g_grid, default map_rows(g, x) — the Dirichlet/initial-data closure at x\nQ Q, default convex_Euclidian_power(T; mg, idx=default_idx(dim), p=xi->p)\ngeometry mg.geometry — the fine-level Geometry\n\nKeyword Arguments\n\ndim::Integer = amg_dim(mg.geometry.discretization): spatial dimension; auto-detected.\nstate_variables = [:u :dirichlet; :s :full], D = default_D(dim): solution components / function spaces and the differential operators applied to them; together they define M.\nx = _xflat(mg): sample points where f/g are evaluated, one row per node.\np::T = T(1.0): p-Laplace exponent for the default Q.\nf/f_grid, g/g_grid: forcing and boundary/initial data, as closures (lowered via map_rows) or as pre-built grids.\nQ::Convex{T}: convex constraint; defaults to a p-Laplace power-cone barrier.\nM: supply an AMG hierarchy pair directly instead of building it from mg.\n\nAny extra (solver-control) keywords are accepted and ignored.\n\n\n\n\n\n","title":"MultiGridBarrier.assemble"},{"category":"function","location":"#MultiGridBarrier.clear_cudss_cache!","page":"Home","text":"clear_cudss_cache!()\n\nDestroy all cached cuDSS factorizations and free associated GPU memory.\n\n\n\n\n\n","title":"MultiGridBarrier.clear_cudss_cache!"},{"category":"method","location":"#MultiGridBarrier.convex_Euclidian_power-Union{Tuple{}, Tuple{Type{T}}, Tuple{T}} where T","page":"Home","text":"convex_Euclidian_power(::Type{T}=Float64; mg, idx=Colon(), A=(x)->I, b=(x)->T(0), p=x->T(2), ...)\n\nCreate a convex set defined by Euclidean norm power constraints, with GPU support.\n\nConstructs a Convex{T} representing the power cone: {y : s ≥ ‖q‖₂^p} where [q; s] = A(x)*y[idx] + b(x)\n\nThis is the fundamental constraint for p-Laplace problems where we need s ≥ ‖∇u‖^p for some scalar field u.\n\nArguments\n\nT::Type=Float64: Numeric type for computations\n\nKeyword Arguments\n\nmg::MultiGrid: Required. The multigrid hierarchy (provides the fine grid).\nidx=Colon(): Indices of y to which transformation applies\nA::Function: Matrix function x -> A(x) for linear transformation\nb::Function: Vector function x -> b(x) for affine shift\np::Function: Exponent function x -> p(x) where p(x) ≥ 1\nA_grid, b_grid, p_grid: Optional pre-computed fine grids (computed from A,b,p if not provided)\n\nReturns\n\nA Convex{T} whose args carry the pre-computed fine parameter grids (A, b, p, μ); the barrier functors receive (args_rows..., y) per vertex via broadcasting (see Convex).\n\nMathematical Details\n\nThe barrier function is:\n\nFor p = 2: -log(s² - ‖q‖²)\nFor p ≠ 2: -log(s^(2/p) - ‖q‖²) - μ(p)*log(s) where μ(p) = 0 if p∈{1,2}, 1 if p<2, 2 if p>2\n\nExamples\n\n# Standard p-Laplace constraint with GPU support. With the default 1D layout\n# Dz = (u, ∂u/∂x, s), idx = SVector(2, 3) selects (q, s) = (∂u/∂x, s).\nmg = amg(fem1d(Float32; nodes=collect(range(-1f0, 1f0, length=33))))\nQ = convex_Euclidian_power(Float32; mg=mg, idx=SVector(2, 3), p=x->1.5f0)\n\n# Q is a single Convex{Float32}\n\n\n\n\n\n","title":"MultiGridBarrier.convex_Euclidian_power"},{"category":"method","location":"#MultiGridBarrier.convex_linear-Union{Tuple{}, Tuple{Type{T}}, Tuple{T}} where T","page":"Home","text":"convex_linear(::Type{T}=Float64; mg, idx=Colon(), A=(x)->I, b=(x)->T(0), A_grid=nothing, b_grid=nothing)\n\nCreate a convex set defined by linear inequality constraints, with GPU support.\n\nConstructs a Convex{T} representing linear constraints. Defines F(y) = A * y[idx] + b where A, b are pre-computed per vertex. The interior is F > 0 (logarithmic barrier applied to each component).\n\nArguments\n\nT::Type=Float64: Numeric type for computations\n\nKeyword Arguments\n\nmg::MultiGrid: Required. The multigrid hierarchy (provides the fine grid).\nidx=Colon(): Indices of y to which constraints apply (default: all)\nA::Function: Matrix function x -> A(x) for constraint coefficients\nb::Function: Vector function x -> b(x) for constraint bounds\nA_grid, b_grid: Optional pre-computed fine grids (computed from A,b if not provided)\n\nReturns\n\nA Convex{T} whose barriers capture the pre-computed fine grids.\n\nExamples\n\nmg = amg(fem1d(Float32; nodes=collect(range(-1f0, 1f0, length=33))))\n\n# Box constraints: -1 ≤ y ≤ 1\nA_box(x) = SMatrix{4,2,Float32}(1,0,-1,0, 0,1,0,-1)\nb_box(x) = SVector{4,Float32}(1, 1, 1, 1)\nQ = convex_linear(Float32; mg=mg, A=A_box, b=b_box, idx=SVector(1, 2))\n\n\n\n\n\n","title":"MultiGridBarrier.convex_linear"},{"category":"method","location":"#MultiGridBarrier.convex_piecewise-Union{Tuple{}, Tuple{Type{T}}, Tuple{T}} where T","page":"Home","text":"convex_piecewise(::Type{T}=Float64; Q::Tuple{Vararg{Convex{T}}}, mg, select::Function=x->(true,...), select_grid=nothing) where {T}\n\nBuild a single Convex{T} that combines multiple convex domains with spatial selectivity.\n\nArguments\n\nQ::Tuple{Vararg{Convex{T}}}: tuple of convex pieces, one Convex{T} each.\nmg::MultiGrid: multigrid hierarchy (provides the fine grid).\nselect::Function: a function x -> Tuple{Bool,...} indicating which pieces are active at spatial position x.\nselect_grid: (optional) pre-computed fine selection grid. If not provided, computed from select.\n\nSemantics\n\nAt each vertex, over the pieces k that are active there (select(x)[k] true):\n\nbarrier   = ∑ₖ Q[k].barrier\ncobarrier = ∑ₖ Q[k].cobarrier\nslack     = maxₖ Q[k].slack\n\nThe slack is the maximum over active pieces, ensuring a single slack value suffices for feasibility.\n\nExamples\n\n# Intersection of two convex domains\nU = convex_Euclidian_power(Float64; mg=mg, idx=SVector(1, 3), p=x->2)\nV = convex_linear(Float64; mg=mg, A=x->A_matrix, b=x->b_vector)\nselect_both(x) = (true, true)\nQint = convex_piecewise(Float64; Q=(U, V), mg=mg, select=select_both)\n\n# Region-dependent constraints\nQ_left = convex_Euclidian_power(Float64; mg=mg, p=x->1.5)\nQ_right = convex_Euclidian_power(Float64; mg=mg, p=x->2.0)\nselect(x) = (x[1] < 0, x[1] >= 0)\nQreg = convex_piecewise(Float64; Q=(Q_left, Q_right), mg=mg, select=select)\n\nSee also: intersect, convex_linear, convex_Euclidian_power.\n\n\n\n\n\n","title":"MultiGridBarrier.convex_piecewise"},{"category":"function","location":"#MultiGridBarrier.cuda_to_native","page":"Home","text":"cuda_to_native(x)\n\nConvert a CUDA Geometry or MGBSOL back to native CPU types.\n\n\n\n\n\n","title":"MultiGridBarrier.cuda_to_native"},{"category":"method","location":"#MultiGridBarrier.default_device!-Tuple{Type{<:Device}}","page":"Home","text":"default_device!(D::Type{<:Device}) -> Type{<:Device}\n\nSet the device mgb_solve uses when device is not given, and return it. Use this to force a backend regardless of what the CUDA extension auto-selected, e.g. default_device!(CPUDevice) to keep a test suite on the CPU even when a GPU is present. A per-call device= keyword still overrides this default.\n\n\n\n\n\n","title":"MultiGridBarrier.default_device!"},{"category":"method","location":"#MultiGridBarrier.default_device-Tuple{}","page":"Home","text":"default_device() -> Type{<:Device}\n\nThe device mgb_solve uses when device is not given. CPUDevice, unless the CUDA extension is loaded and CUDA.functional(), in which case CUDADevice. Pass device=CPUDevice/device=CUDADevice to mgb_solve to override per call.\n\n\n\n\n\n","title":"MultiGridBarrier.default_device"},{"category":"function","location":"#MultiGridBarrier.device_to_native","page":"Home","text":"device_to_native(D::Type{<:Device}, x)\n\nMove x from device D back to native (CPU) types. CPUDevice is the identity; the CUDA extension supplies the CUDADevice method, delegating to cuda_to_native. The inverse is native_to_device.\n\n\n\n\n\n","title":"MultiGridBarrier.device_to_native"},{"category":"method","location":"#MultiGridBarrier.fem1d-Union{Tuple{}, Tuple{Type{T}}, Tuple{T}} where T","page":"Home","text":"fem1d(::Type{T}=Float64; nodes, k=1, K=<from nodes>, ambient=Val(1), t=nothing) -> Geometry\n\nConstruct a single-level 1D Qk FEM Geometry. Each element carries k+1 Lagrange-Chebyshev nodes; k=1 reproduces the legacy P1 discretization exactly. nodes is the strictly increasing vector of element endpoints (the default mesh is the straight Qk element on each [nodes[i], nodes[i+1]]).\n\nThe map is isoparametric: pass K as the full (k+1, n_e, 1) Lagrange-node tensor to give each element a nontrivial 1D parametrization (interior nodes off the affine positions), or as the (2, n_e, 1) endpoint tensor for straight elements. K may be passed on its own — nodes is only needed when K is not given.\n\nThe curve may be embedded in a higher ambient dimension e ∈ {2,3}: pass ambient=Val(2) or ambient=Val(3) (default Val(1)) together with a mesh K of that many coordinate columns, and fem1d builds a 1-manifold (curve) in ℝ^e of type TensorFEM{1,e,T}. The operators :dx, :dy[, :dz] are then the e ambient components of the intrinsic (arc-length) gradient — each tangent to the curve — and the quadrature weights are the arc-length measure √det(JᵀJ). Closed curves (circles, loops) glue automatically by coordinate dedup; this reduces exactly to the e=1 case on the diagonal.\n\nPass t (a (k+1, n_e) Integer matrix; t[v,e] is the global id of local node v in element e) to supply full-node connectivity explicitly instead of recovering it by coordinate dedup — needed when geometrically-coincident nodes must stay topologically distinct (slit domains, branch cuts, glued manifolds). Build one from corner connectivity with tensor_dofmap.\n\nAttach a hierarchy with amg(geom).\n\n\n\n\n\n","title":"MultiGridBarrier.fem1d"},{"category":"method","location":"#MultiGridBarrier.fem2d-Union{Tuple{}, Tuple{Type{T}}, Tuple{T}} where T","page":"Home","text":"fem2d(::Type{T}=Float64; k=1, K=<unit square>, ambient=Val(2), t=nothing) -> Geometry\n\nConstruct a single-level 2D Q_k FEM Geometry on quadrilaterals (k=1 is bilinear Q1). The map is isoparametric: pass K as the full ((k+1)^2, N, 2) Lagrange-node tensor (tensor order, axis-1 fastest) for curved quads, or as the (4, N, 2) Q1-corner tensor — order (-1,-1), (+1,-1), (-1,+1), (+1,+1) — for straight quads.\n\nThe quads may be embedded in ℝ³: pass ambient=Val(3) (default Val(2)) together with a mesh K of 3 coordinate columns, and fem2d builds a 2-manifold (surface) of type TensorFEM{2,3,T}. The operators :dx, :dy, :dz are then the three ambient components of the intrinsic tangential gradient ∇_Γ — tangent to the surface, so n·∇_Γ = 0 holds by construction — and the weights are the surface-area measure √det(JᵀJ). Closed surfaces (spheres, tori) glue by coordinate dedup. This reduces exactly to the planar discretization when e=2.\n\nPass t (a ((k+1)^2, N) Integer matrix; t[v,e] is the global id of local node v in element e) to supply full-node connectivity explicitly instead of recovering it by coordinate dedup — needed for slit domains, branch cuts, and glued manifolds, where geometrically-coincident nodes must stay topologically distinct. Build one from corner connectivity with tensor_dofmap.\n\nAttach a hierarchy with amg(geom).\n\n\n\n\n\n","title":"MultiGridBarrier.fem2d"},{"category":"method","location":"#MultiGridBarrier.fem2d_P1-Union{Tuple{}, Tuple{Type{T}}, Tuple{T}} where T","page":"Home","text":"fem2d_P1(::Type{T}=Float64; K=<default unit-square>) -> Geometry\n\nConstruct a single-level 2D FEM P1 Geometry on the doubled-per-element fine triangulation K. Use amg(geom) to attach an algebraic-multigrid hierarchy. (The legacy geometric_mg(geom, L) builds geometric-subdivision transfers instead.)\n\nArguments\n\nK::Array{T,3} (3 × N × 2): per-triangle corner tensor; K[v, t, d] is coordinate d of vertex v of triangle t.\n\nBoundary conditions are chosen later, at amg time, via its dirichlet_nodes keyword (the default constrains the whole boundary).\n\n\n\n\n\n","title":"MultiGridBarrier.fem2d_P1"},{"category":"method","location":"#MultiGridBarrier.fem2d_P2-Union{Tuple{}, Tuple{Type{T}}, Tuple{T}} where T","page":"Home","text":"fem2d_P2(::Type{T}=Float64; K=<default 7-DOF unit square>) -> Geometry\n\nConstruct a single-level 2D FEM Geometry on the doubled P2+bubble mesh K (7 × N × 2). Use amg(geom) to attach an algebraic-multigrid hierarchy. (The legacy geometric_mg(geom, L) builds geometric-subdivision transfers instead.)\n\nArguments\n\nK::Array{T,3} (7 × N × 2): P2+bubble per-triangle mesh; the 7 vertices per triangle are laid out as corner1, edge(1,2), corner2, edge(2,3), corner3, edge(3,1), centroid.\n\nThe element geometry is isoparametric: the map from the reference triangle is built from all 7 node positions via the P2+bubble shape functions, so displacing the edge/centroid nodes off the straight midpoints/barycenter genuinely curves the element (with a node-varying Jacobian). Triangles must be orientation-preserving and non-self-intersecting — construction errors if any element's det J ≤ 0 at a quadrature node, since the barrier method requires strictly positive weights.\n\n\n\n\n\n","title":"MultiGridBarrier.fem2d_P2"},{"category":"method","location":"#MultiGridBarrier.fem3d-Union{Tuple{}, Tuple{Type{T}}, Tuple{T}} where T","page":"Home","text":"fem3d(::Type{T}=Float64; k=3, K=<unit cube>, t=nothing) -> Geometry\n\nConstruct a single-level 3D Q_k FEM Geometry on hexahedra. The map is isoparametric: pass K as the full ((k+1)^3, N, 3) Lagrange-node tensor (tensor order, axis-1 fastest) so displacing edge/face/interior nodes curves the hex (node-varying Jacobian), or as the (8, N, 3) Q1-corner tensor for straight hexes. The default is a single straight unit cube.\n\nPass t (a ((k+1)^3, N) Integer matrix; t[v,e] is the global id of local node v in element e) to supply full-node connectivity explicitly instead of recovering it by coordinate dedup (slit domains / glued manifolds). tensor_dofmap builds one from corner connectivity, though for k≥3 in 3D it does not yet number shared face-interior grids (it throws) — there, supply t by hand or use the dedup default.\n\nAttach a hierarchy with amg(geom).\n\n\n\n\n\n","title":"MultiGridBarrier.fem3d"},{"category":"function","location":"#MultiGridBarrier.find_boundary","page":"Home","text":"find_boundary(geom::Geometry) -> Vector{Tuple{Int,Int}}\n\nReturn the (v, e) index pairs of the mesh nodes on ∂Ω, in the per-element layout of geom.x (the 3-tensor of shape (V, N, D)): v is the local vertex index within element e. Duplicates are present — a corner shared by k elements contributes its k pairs (one per element that owns it).\n\namg(geom; dirichlet_nodes=Dict(:sym => set, …)) consumes these (v, e) pairs: each value set is a Vector{Tuple{Int,Int}} of the nodes constrained to zero trace in subspace :sym. A geometric position is treated as Dirichlet for that subspace iff any pair at that position is in set, so you can pass either the full set returned by find_boundary (or a subset) or a sparser representative-only set.\n\nDefined for each FEM discretization (the tensor-product family FEM1D/FEM2D/FEM3D, plus FEM2D_P1 and FEM2D_P2). For spectral discretizations the zero-trace subspace is built by basis truncation rather than by node masking; the spectral amg does not accept dirichlet_nodes and find_boundary returns the perimeter Chebyshev nodes (paired with the single notional element index 1) for reference only.\n\nEmpty or singleton node sets are allowed; the resulting AMG problem may still be well-posed if the variational form carries a mass term.\n\n\n\n\n\n","title":"MultiGridBarrier.find_boundary"},{"category":"method","location":"#MultiGridBarrier.find_boundary-Union{Tuple{Geometry{T, <:Any, <:Any, <:Any, FEM2D_P1{T}}}, Tuple{T}} where T","page":"Home","text":"find_boundary(geom::Geometry{...,FEM2D_P1{T}}) -> Vector{Tuple{Int,Int}}\n\n(v, t) index pairs into geom.x for every vertex on ∂Ω. A corner shared by k triangles contributes its k pairs (one per triangle that owns it).\n\n\n\n\n\n","title":"MultiGridBarrier.find_boundary"},{"category":"method","location":"#MultiGridBarrier.find_boundary-Union{Tuple{Geometry{T, <:Any, <:Any, <:Any, FEM2D_P2{T}}}, Tuple{T}} where T","page":"Home","text":"find_boundary(geom::Geometry{...,FEM2D_P2{T}}) -> Vector{Tuple{Int,Int}}\n\n(v, t) index pairs into geom.x for every P2+bubble DOF (corner vertex or edge midpoint) on ∂Ω. Centroids never appear (they are interior by construction). Duplicates are present (a corner shared by k triangles contributes its k pairs; a boundary-edge midpoint contributes its single pair).\n\n\n\n\n\n","title":"MultiGridBarrier.find_boundary"},{"category":"method","location":"#MultiGridBarrier.find_boundary-Union{Tuple{Geometry{T, <:Any, <:Any, <:Any, SPECTRAL1D{T}}}, Tuple{T}} where T","page":"Home","text":"find_boundary(geom::Geometry{...,SPECTRAL1D{T}}) -> Vector{Tuple{Int,Int}}\n\nThe two (v, t) pairs [(1, 1), (n, 1)] for the endpoint Chebyshev nodes. Spectral geometries use a single notional element (size(geom.x, 2) == 1) since there is no real per-element structure; the flattened view has one row per unique node. Informational only: the spectral amg builds the zero-trace subspace by basis truncation, not node masking; it does not accept dirichlet_nodes.\n\n\n\n\n\n","title":"MultiGridBarrier.find_boundary"},{"category":"method","location":"#MultiGridBarrier.find_boundary-Union{Tuple{Geometry{T, <:Any, <:Any, <:Any, SPECTRAL2D{T}}}, Tuple{T}} where T","page":"Home","text":"find_boundary(geom::Geometry{...,SPECTRAL2D{T}}) -> Vector{Tuple{Int,Int}}\n\n(v, 1) pairs (single notional element) for every Chebyshev node on the perimeter of the tensor-product spectral 2D mesh. Informational only: the spectral amg builds the zero-trace subspace by basis truncation, not node masking; it does not accept dirichlet_nodes.\n\n\n\n\n\n","title":"MultiGridBarrier.find_boundary"},{"category":"method","location":"#MultiGridBarrier.find_boundary-Union{Tuple{Geometry{T, <:Any, <:Any, <:Any, TensorFEM{d, E, T}}}, Tuple{E}, Tuple{d}, Tuple{T}} where {T, d, E}","page":"Home","text":"find_boundary(geom::Geometry{...,TensorFEM{d,e,T}}) -> Vector{Tuple{Int,Int}}\n\n(v, e) index pairs into geom.x for every Q_k DOF on ∂Ω. A (d-1)-face used by exactly one element is on the boundary; every DOF on such a face is returned (corner / edge / face-interior, including the k≥2 interior-of-face nodes).\n\n\n\n\n\n","title":"MultiGridBarrier.find_boundary"},{"category":"function","location":"#MultiGridBarrier.geometric_mg","page":"Home","text":"geometric_mg(geom::Geometry, L::Int) -> MultiGrid\n\nBuild a geometric-subdivision multigrid hierarchy of L levels on top of geom. The returned MultiGrid's geometry is the finest mesh (after L-1 levels of subdivision).\n\nThe spectral discretizations have no geometric subdivision: their geometric_mg ignores L and returns the same hierarchy as amg (so subdivide is a no-op for them).\n\n\n\n\n\n","title":"MultiGridBarrier.geometric_mg"},{"category":"function","location":"#MultiGridBarrier.interpolate","page":"Home","text":"interpolate(M::Geometry, z::Vector, t)\n\nInterpolate a solution vector at specified points.\n\nGiven a solution z on the mesh M, evaluates the solution at new points t using the appropriate interpolation method for the discretization.\n\nSupported discretizations\n\n1D FEM (FEM1D): exact per-element degree-k Lagrange (Q_k) interpolation (piecewise linear at k = 1); evaluation points outside the mesh are clamped to the boundary values\n1D spectral (SPECTRAL1D): spectral polynomial interpolation\n2D spectral (SPECTRAL2D): tensor-product spectral interpolation\n\nNote: 2D/3D FEM interpolation is not currently provided.\n\nArguments\n\nM::Geometry: The geometry containing grid and basis information\nz::Vector: Solution vector on the finest grid (length must match number of DOFs)\nt: Evaluation points. Format depends on dimension:\n1D: scalar or Vector{T} of x-coordinates\n2D spectral: Matrix{T} where each row is [x, y]\n\nReturns\n\nInterpolated values at the specified points. Shape matches input t.\n\nExamples\n\n# 1D interpolation (FEM)\ngeom = subdivide(fem1d(; nodes=collect(range(-1.0, 1.0, length=3))), 3)\nz = sin.(π .* vec(geom.x))\ny = interpolate(geom, z, 0.5)\ny_vec = interpolate(geom, z, [-0.5, 0.0, 0.5])\n\n# 2D interpolation (spectral)\ngeom = spectral2d(n=4)\nxf = reshape(geom.x, :, size(geom.x, 3))   # flat (n_nodes, 2) view\nz = exp.(-xf[:,1].^2 .- xf[:,2].^2)\npoints = [0.0 0.0; 0.5 0.5; -0.5 0.5]\nvals = interpolate(geom, z, points)\n\n\n\n\n\n","title":"MultiGridBarrier.interpolate"},{"category":"method","location":"#MultiGridBarrier.linesearch_backtracking-Union{Tuple{}, Tuple{Type{T}}, Tuple{T}} where T","page":"Home","text":"linesearch_backtracking(::Type{T}=Float64; beta=T(0.5)) where {T}\n\nCreate a backtracking line search function for Newton methods.\n\nArguments\n\nT : numeric type for computations (default: Float64).\n\nKeyword arguments\n\nbeta : backtracking parameter for step size reduction (default: 0.5).\nc1 : Armijo sufficient-decrease constant c₁ (default: 0.1).\n\nReturns\n\nA line search function ls(x, y, g, n, F0, F1; printlog) where:\n\nx : current point (vector of type T).\ny : current objective value F0(x).\ng : current gradient F1(x).\nn : search direction (typically Newton direction H\\g).\nF0 : objective function.\nF1 : gradient function.\nprintlog : logging function.\n\nReturns (xnext, ynext, gnext) where xnext = x - s*n for some step size s.\n\nAlgorithm\n\nImplements the Armijo backtracking line search with sufficient decrease condition: F(x - s*n) ≤ F(x) - c₁ * s * ⟨∇F(x), n⟩, where c₁ is the keyword c1 (default 0.1). The step size starts at s = 1 and is reduced by factor beta until the condition is satisfied or numerical limits are reached.\n\nNotes\n\nThis is a robust and commonly used line search that guarantees sufficient decrease in the objective function, making it suitable for general nonlinear optimization.\n\n\n\n\n\n","title":"MultiGridBarrier.linesearch_backtracking"},{"category":"method","location":"#MultiGridBarrier.linesearch_illinois-Union{Tuple{}, Tuple{Type{T}}, Tuple{T}} where T","page":"Home","text":"linesearch_illinois(::Type{T}=Float64; beta=T(0.5)) where {T}\n\nCreate an Illinois-based line search function for Newton methods.\n\nArguments\n\nT : numeric type for computations (default: Float64).\n\nKeyword arguments\n\nbeta : backtracking parameter for step size reduction when Illinois fails (default: 0.5).\n\nReturns\n\nA line search function ls(x, y, g, n, F0, F1; printlog) where:\n\nx : current point (vector of type T).\ny : current objective value F0(x).\ng : current gradient F1(x).\nn : Newton direction (typically H\\g where H is the Hessian).\nF0 : objective function.\nF1 : gradient function.\nprintlog : logging function.\n\nReturns (xnext, ynext, gnext) where xnext = x - s*n for some step size s.\n\nAlgorithm\n\nThe Illinois algorithm finds a root of φ(s) = ⟨∇F(x - s*n), n⟩, which corresponds to the exact line search condition. If the Illinois solver fails or encounters numerical issues, the step size is reduced by factor beta and the process repeats.\n\nNotes\n\nThis line search strategy aims for the exact minimizer along the search direction, making it potentially more aggressive than backtracking but also more expensive per iteration.\n\n\n\n\n\n","title":"MultiGridBarrier.linesearch_illinois"},{"category":"method","location":"#MultiGridBarrier.mgb_solve-Union{Tuple{MGBProblem{T}}, Tuple{T}} where T","page":"Home","text":"mgb_solve(prob::MGBProblem; device=default_device(), kwargs...) -> MGBSOL\n\nMultiGrid Barrier (MGB) solver for nonlinear convex optimization problems on a multigrid hierarchy. Operates in a feasibility phase followed by a main optimization phase, with damped Newton inner solves and line search.\n\nSolves an assembled MGBProblem — build one with assemble (where the problem-specification keywords p, f, g, Q, state_variables, D, … live), or take one from the Zoo. The problem is moved to device, solved there, and the solution moved back, so the returned MGBSOL is always in native CPU types regardless of device.\n\nKeyword Arguments\n\nBackend\n\ndevice::Type{<:Device} = default_device(): compute backend, CPUDevice (default) or CUDADevice (requires using CUDA, CUDSS_jll). The problem is moved to the device, solved there, and the solution moved back; the returned MGBSOL is always native.\n\nOutput Control\n\nverbose::Bool = true: progress bar.\nlogfile = devnull: optional log stream.\n\nSolver Control (forwarded to mgb_driver)\n\ntol, t, t_feasibility, maxit, kappa, early_stop, max_newton, stopping_criterion, line_search, finalize, progress, printlog.\n\nReturns\n\nAn MGBSOL whose z is the fine-level solution matrix and whose geometry is the fine-level Geometry (the MultiGrid itself is not stored).\n\nExamples\n\nsol = mgb_solve(assemble(amg(fem1d(; nodes = collect(range(-1.0, 1.0, length=33)))); p = 1.5))\nsol = mgb_solve(assemble(amg(subdivide(fem2d_P2(), 3)); p = 1.5))\nsol = mgb_solve(assemble(amg(spectral2d(n = 8)); p = 2.0); device = CPUDevice)\nsol = mgb_solve(Zoo.p_harmonic(amg(fem2d_P2()); p = 1.5); tol = 1e-4)\n\n\n\n\n\n","title":"MultiGridBarrier.mgb_solve"},{"category":"function","location":"#MultiGridBarrier.native_to_cuda","page":"Home","text":"native_to_cuda(prob::MGBProblem) -> MGBProblem\nnative_to_cuda(geom::Geometry) -> Geometry\n\nConvert a native (CPU) assembled MGBProblem — or one of its pieces: a Geometry, an AMG hierarchy, a Convex, raw arrays — to CUDA GPU types, faithfully preserving matrix types: BlockDiag operators stay block (driving the structured batched-GEMM Hessian assembly), sparse matrices become CuSparseMatrixCSR. FEM geometries always carry BlockDiag operators, so the solve is structured; only the spectral discretizations use dense operators. Requires using CUDA, CUDSS_jll.\n\n\n\n\n\n","title":"MultiGridBarrier.native_to_cuda"},{"category":"function","location":"#MultiGridBarrier.native_to_device","page":"Home","text":"native_to_device(D::Type{<:Device}, x)\n\nMove x from native (CPU) types onto device D. CPUDevice is the identity; the CUDA extension supplies the CUDADevice method, delegating to native_to_cuda. The inverse is device_to_native.\n\n\n\n\n\n","title":"MultiGridBarrier.native_to_device"},{"category":"method","location":"#MultiGridBarrier.parabolic_solve-Union{Tuple{}, Tuple{MultiGrid{T, M_R, G} where {M_R, G<:(Geometry{T, X} where X<:AbstractArray{T, 3})}}, Tuple{T}} where T","page":"Home","text":"parabolic_solve(mg::MultiGrid; kwargs...) -> ParabolicSOL\n\nSolve time-dependent p-Laplace problems using implicit Euler timestepping on the multigrid hierarchy mg.\n\nKeyword Arguments\n\nDiscretization\n\nstate_variables = [:u :dirichlet; :s1 :full; :s2 :full].\nD: differential operators (default depends on spatial dimension).\ndim::Int: spatial dimension (auto-detected from mg).\n\nTime Integration\n\nt0=T(0), t1=T(1), h=T(0.2), ts=t0:h:t1.\n\nProblem Parameters\n\np::T=T(1): exponent for the p-Laplacian.\nf1: source term (t, x) -> T (default: (t,x)->T(0.5)).\ng: initial/boundary conditions (t, x) -> Vector{T}.\nQ: convex constraints.\n\nAdvanced (grid-level) overrides\n\nf1_grid: per-node samples of f1 at every ts[j] (one column per time).\ng_grid: a function j -> grid, the sampled g at time ts[j].\nf_grid: a function (z, j) -> grid, the linear-term grid for step j given the previous state z; together with f_default (the per-row layout of that linear functional) the default implements implicit Euler for the p-Laplace flow.\n\nOutput Control\n\nverbose::Bool=true: progress bar.\n\nAdditional Parameters\n\nrest...: forwarded to mgb_solve for each time step.\n\nExamples\n\nsol = parabolic_solve(amg(fem2d_P2()); p=2.0, h=0.1)\nsol = parabolic_solve(amg(fem1d(; nodes=collect(range(-1.0, 1.0, length=33)))); p=1.5, h=0.05)\n\n\n\n\n\n","title":"MultiGridBarrier.parabolic_solve"},{"category":"method","location":"#MultiGridBarrier.spectral1d-Union{Tuple{}, Tuple{Type{T}}, Tuple{T}} where T","page":"Home","text":"spectral1d(::Type{T}=Float64; n=16) -> Geometry\n\nConstruct a 1D spectral single-level Geometry with n Chebyshev nodes. Use amg(geom) to attach a multigrid hierarchy.\n\n\n\n\n\n","title":"MultiGridBarrier.spectral1d"},{"category":"method","location":"#MultiGridBarrier.spectral2d-Union{Tuple{}, Tuple{Type{T}}, Tuple{T}} where T","page":"Home","text":"spectral2d(::Type{T}=Float64; n=4) -> Geometry\n\nConstruct a 2D tensor-Chebyshev spectral single-level Geometry. Use amg(geom) to attach the spectral multigrid hierarchy.\n\n\n\n\n\n","title":"MultiGridBarrier.spectral2d"},{"category":"method","location":"#MultiGridBarrier.stopping_exact-Tuple{T} where T","page":"Home","text":"stopping_exact(theta::T) where {T}\n\nCreate an exact stopping criterion for Newton methods.\n\nArguments\n\ntheta : tolerance parameter for gradient norm relative decrease (type T).\n\nReturns\n\nA stopping criterion function with signature: stop(ymin, ynext, gmin, gnext, n, ndecmin, ndec) -> Bool\n\nwhere:\n\nymin : minimum objective value seen so far.\nynext : current objective value.\ngmin : minimum gradient norm seen so far.\ngnext : current gradient vector.\nn : current Newton direction.\nndecmin : square root of minimum Newton decrement seen so far.\nndec : square root of current Newton decrement.\n\nAlgorithm\n\nReturns true (stop) if both conditions hold:\n\nNo objective improvement: ynext ≥ ymin\nGradient norm stagnation: ‖gnext‖ ≥ theta * gmin\n\nNotes\n\nThis criterion is \"exact\" in the sense that it requires both objective and gradient stagnation before stopping, making it suitable for high-precision optimization. Typical values of theta are in the range [0.1, 0.9].\n\n\n\n\n\n","title":"MultiGridBarrier.stopping_exact"},{"category":"method","location":"#MultiGridBarrier.stopping_inexact-Union{Tuple{T}, Tuple{T, T}} where T","page":"Home","text":"stopping_inexact(lambda_tol::T, theta::T) where {T}\n\nCreate an inexact stopping criterion for Newton methods that combines Newton decrement and exact stopping conditions.\n\nArguments\n\nlambda_tol : tolerance for the Newton decrement (type T).\ntheta : tolerance parameter for the exact stopping criterion (type T).\n\nReturns\n\nA stopping criterion function with signature: stop(ymin, ynext, gmin, gnext, n, ndecmin, ndec) -> Bool\n\nwhere:\n\nymin : minimum objective value seen so far.\nynext : current objective value.\ngmin : minimum gradient norm seen so far.\ngnext : current gradient vector.\nn : current Newton direction.\nndecmin : square root of minimum Newton decrement seen so far.\nndec : square root of current Newton decrement (√(gᵀH⁻¹g)).\n\nAlgorithm\n\nReturns true (stop) if either condition holds:\n\nNewton decrement condition: ndec < lambda_tol\nExact stopping condition: stopping_exact(theta) is satisfied\n\nNotes\n\nThis criterion is \"inexact\" because it allows early termination based on the Newton decrement, which provides a quadratic convergence estimate. The Newton decrement λ = √(gᵀH⁻¹g) approximates the distance to the optimum in the Newton metric. Typical values: lambda_tol ∈ [1e-6, 1e-3], theta ∈ [0.1, 0.9].\n\n\n\n\n\n","title":"MultiGridBarrier.stopping_inexact"},{"category":"method","location":"#MultiGridBarrier.subdivide-Tuple{Geometry, Int64}","page":"Home","text":"subdivide(geom::Geometry, L::Int) -> Geometry\n\nRefine geom's mesh by L-1 levels of geometric subdivision and return a new fine-mesh Geometry, discarding the transfer operators that the geometric MG construction would otherwise produce. For FEM discretizations the fine operators are BlockDiag, so a subsequent amg(subdivide(geom, L)) runs the batched-GEMM (structured) Hessian assembly via the D_fine path.\n\n\n\n\n\n","title":"MultiGridBarrier.subdivide"},{"category":"method","location":"#MultiGridBarrier.tensor_dofmap-Union{Tuple{d}, Tuple{AbstractMatrix{<:Integer}, Int64, Val{d}}} where d","page":"Home","text":"tensor_dofmap(t_corner, k, ::Val{d}) -> t\n\nTopological full-node connectivity for a d-dimensional Qₖ tensor mesh, built from corner connectivity alone — no coordinates — so coincident-but-distinct nodes (slits, branch cuts, glued manifolds) are preserved exactly. This is the import path that the coordinate-dedup constructor cannot express.\n\nt_corner is the (2^d, N) corner connectivity: t_corner[c, e] is the global id of corner c of element e, in the package corner order (bit a-1 of c-1 selects the low/high end of axis a, matching _tf_corner_local). The returned t is ((k+1)^d, N) in element-local node order (axis-1 fastest), suitable as the t= argument to fem1d/fem2d/fem3d.\n\nNumbering: corner ids carry through unchanged; shared edges/faces are matched by their corner-id set; cell-interior nodes get fresh ids. Edge-interior nodes (k≥3) are oriented by the global ids of the two endpoints, so two elements that traverse a shared edge in opposite local order still agree.\n\nSupported: any d for k≤2; any k for d≤2. Not yet supported: shared faces carrying interior grids (d≥3 and k≥3), which need 2D face-orientation matching — use the coordinate-dedup constructor for embedded 3D high-order meshes (it handles those; it just cannot represent slits).\n\n\n\n\n\n","title":"MultiGridBarrier.tensor_dofmap"},{"category":"function","location":"#PyPlot.plot","page":"Home","text":"plot(sol::MGBSOL, k::Int=1; kwargs...)\nplot(sol::ParabolicSOL, k::Int=1; kwargs...)\nplot(M::Geometry, z::Vector; kwargs...)\nplot(M::Geometry, ts::AbstractVector, U::Matrix; frame_time=..., embed_limit=..., printer=...)\n\nVisualize solutions and time sequences on meshes.\n\n1D problems: Line plot. For spectral methods, you can specify evaluation points with x=-1:0.01:1.\n2D FEM: Triangulated surface plot using the mesh structure.\n2D spectral: 3D surface plot. You can specify evaluation grids with x=-1:0.01:1, y=-1:0.01:1.\n\nTime sequences (animation):\n\nCall plot(M, ts, U; frame_time=1/30, printer=anim->nothing) where U has columns as frames and ts are absolute times in seconds (non-uniform allowed).\nOr simply call plot(sol) where sol is a ParabolicSOL returned by parabolic_solve (uses sol.ts).\nAnimation advances at a fixed frame rate given by frame_time (seconds per video frame). For irregular ts, each video frame shows the latest data frame with timestamp ≤ current video time.\nThe printer callback receives the Matplotlib animation object; use it to display or save (e.g., anim.save(\"out.mp4\")).\nembed_limit controls the maximum embedded HTML5 video size in megabytes.\n\nWhen sol is a solution object returned by mgb_solve, plot(sol,k) plots the kth component sol.z[:, k] using sol.geometry. plot(sol) uses the default k=1.\n\nAll other keyword arguments are passed to the underlying PyPlot functions.\n\n\n\n\n\n","title":"PyPlot.plot"},{"category":"method","location":"#PyPlot.plot-Union{Tuple{E}, Tuple{W}, Tuple{X}, Tuple{T}, Tuple{Geometry{T, X, W, <:Any, FEM3D{E, T}}, AbstractVector, Matrix{T}}} where {T, X, W, E}","page":"Home","text":"plot(M::Geometry{...,FEM3D}, ts::AbstractVector, U::Matrix; frame_time, kwargs...)\n\nAnimate a time series of 3D solutions (columns of U at times ts) into an HTML5anim MP4 via ffmpeg. Color limits and isosurfaces are fixed across frames from the global range of U.\n\n\n\n\n\n","title":"PyPlot.plot"},{"category":"method","location":"#PyPlot.plot-Union{Tuple{E}, Tuple{W}, Tuple{X}, Tuple{T}, Tuple{Geometry{T, X, W, <:Any, FEM3D{E, T}}, Vector{T}}} where {T, X, W, E}","page":"Home","text":"plot(geo::Geometry{...,FEM3D}, u::Vector; kwargs...)\n\nPlot a 3D Q_k solution u on a hexahedral geometry using PyVista (volume render + isosurfaces, with optional slices). Returns an MGB3DFigure (PNG). See the keyword arguments below for volume/isosurface/slice options.\n\nKeyword Arguments\n\nplotter=(window_size=(800, 600),): options passed to pv.Plotter().\nvolume=(;): options for add_volume; pass nothing to disable.\nscalar_bar_args=...: options for add_scalar_bar; pass nothing to hide.\nisosurfaces: isosurface values (default: 5 evenly spaced; [] to disable).\ncontour_mesh, slice, slice_orthogonal, slice_along_axis, show_grid, camera_position: see the slicing/grid options.\n\n\n\n\n\n","title":"PyPlot.plot"},{"category":"method","location":"#PyPlot.plot-Union{Tuple{MultiGridBarrier.ParabolicSOL{T, X, W, <:Any, <:Geometry{T, <:Any, <:Any, <:Any, FEM3D{E, T}}}}, Tuple{E}, Tuple{W}, Tuple{X}, Tuple{T}, Tuple{MultiGridBarrier.ParabolicSOL{T, X, W, <:Any, <:Geometry{T, <:Any, <:Any, <:Any, FEM3D{E, T}}}, Int64}} where {T, X, W, E}","page":"Home","text":"plot(sol::ParabolicSOL, k::Int=1; kwargs...)\n\nAnimate a 3D parabolic solution (component k).\n\n\n\n\n\n","title":"PyPlot.plot"},{"category":"method","location":"#PyPlot.plot-Union{Tuple{W}, Tuple{X}, Tuple{T}, Tuple{Geometry{T, X, W, <:Any, FEM1D{2, T}}, Vector{T}}} where {T, X, W}","page":"Home","text":"plot(geo::Geometry{...,TensorFEM{1,e,T}}, z::Vector; tube=(;), height_scale=1, kwargs...)\n\nPlot a scalar field z on a Q_k curve with PyVista, drawn as a tube colored by z. Following the package's \"graph the solution\" convention (cf. the d=1 line and d=2 surface plots), the solution occupies the first free ambient dimension:\n\na curve in ℝ² (ambient=Val(2)) is rendered as the height-graph (x, y, z) — the solution becomes the third coordinate (use height_scale to exaggerate or compress it relative to the spatial extent; default 1, i.e. 1:1 with the data);\na curve in ℝ³ (ambient=Val(3)) is rendered in place — there is no spare dimension, so z shows up as color only.\n\nReturns an MGB3DFigure (PNG). Pass tube=nothing for bare lines, or tube=(radius=r,) to set the radius (default ≈ 1% of the bounding-box diagonal).\n\n\n\n\n\n","title":"PyPlot.plot"},{"category":"method","location":"#PyPlot.plot-Union{Tuple{W}, Tuple{X}, Tuple{T}, Tuple{Geometry{T, X, W, <:Any, FEM2D{3, T}}, Vector{T}}} where {T, X, W}","page":"Home","text":"plot(geo::Geometry{...,TensorFEM{2,3,T}}, z::Vector; kwargs...)\n\nPlot a scalar field z on a Q_k surface in ℝ³ (a fem2d(…; ambient=Val(3)) geometry) with PyVista — the quad mesh drawn as a colored surface. Returns an MGB3DFigure (PNG).\n\nKeyword Arguments\n\nplotter=(window_size=(800,600),): options passed to pv.Plotter().\nmesh=(;): extra options for add_mesh (e.g. show_edges=true, cmap=\"viridis\").\nscalar_bar_args=…: options for add_scalar_bar; pass nothing to hide.\nshow_grid=true, camera_position=nothing.\n\n\n\n\n\n","title":"PyPlot.plot"},{"category":"method","location":"#PyPlot.savefig-Tuple{MGB3DFigure, String}","page":"Home","text":"savefig(fig::MGB3DFigure, filename::String)\n\nSave the figure to a file (e.g., \"plot.png\"). Extends PyPlot.savefig.\n\n\n\n\n\n","title":"PyPlot.savefig"},{"category":"section","location":"zoo/#The-Zoo:-a-menu-of-convex-variational-problems","page":"Zoo","text":"MultiGridBarrier.Zoo packages six classical convex variational problems as one-line constructors that return an MGBProblem. The typical usage is\n\nmg      = amg(fem2d_P1())\nproblem = Zoo.minimal_surface(mg)\nsol     = mgb_solve(problem)\nplot(sol)\n\nEach problem ships with dimension-agnostic defaults; the same constructor works for a 1D, 2D, or 3D mesh. Below: one solve per problem at each dimension, on the coarsest mesh that still draws an interesting picture. Tolerances are loose (tol = 1e-3) and progress bars are suppressed.\n\nusing PyPlot  # hide\nusing MultiGridBarrier\nnodes1 = collect(range(-1.0, 1.0, length=17))     # 1D\nmg1 = amg(fem1d(; nodes=nodes1))\nmg2 = amg(subdivide(fem2d_P1(), 3))               # 2D\nmg3 = amg(subdivide(fem3d(; k=1), 3))             # 3D — L=3 to resolve active sets\nnothing  # hide","title":"The Zoo: a menu of convex variational problems"},{"category":"section","location":"zoo/#1.-Elasto-plastic-torsion-(Hencky)","page":"Zoo","text":"min int tfrac12nabla u^2 + f u  dx\nquad textstquad nabla u leq textttsmax\n\nA twisted prismatic bar in the elastic-perfectly-plastic regime; the constraint is the von Mises yield bound on the stress potential.","title":"1. Elasto-plastic torsion (Hencky)"},{"category":"section","location":"zoo/#1D","page":"Zoo","text":"sol = mgb_solve(Zoo.elastoplastic_torsion(mg1); verbose=false, tol=1e-3)\nplot(sol); savefig(\"zoo_torsion_1d.svg\"); nothing  # hide\nclose()  # hide\n\n(Image: )","title":"1D"},{"category":"section","location":"zoo/#2D","page":"Zoo","text":"sol = mgb_solve(Zoo.elastoplastic_torsion(mg2); verbose=false, tol=1e-3)\nplot(sol); savefig(\"zoo_torsion_2d.svg\"); nothing  # hide\nclose()  # hide\n\n(Image: )","title":"2D"},{"category":"section","location":"zoo/#3D","page":"Zoo","text":"sol = mgb_solve(Zoo.elastoplastic_torsion(mg3); verbose=false, tol=1e-3)\nfig = plot(sol); savefig(fig, \"zoo_torsion_3d.png\"); nothing  # hide\n\n(Image: )","title":"3D"},{"category":"section","location":"zoo/#2.-Minimal-surface-(graph-form)","page":"Zoo","text":"min int sqrt1 + nabla u^2 dx\n\nwith prescribed Dirichlet boundary trace. Plateau / Bernstein problem. The dim-aware default g_u gives a non-trivial trace so the picture isn't u equiv 0.","title":"2. Minimal surface (graph form)"},{"category":"section","location":"zoo/#1D-2","page":"Zoo","text":"sol = mgb_solve(Zoo.minimal_surface(mg1); verbose=false, tol=1e-3)\nplot(sol); savefig(\"zoo_minsurf_1d.svg\"); nothing  # hide\nclose()  # hide\n\n(Image: )","title":"1D"},{"category":"section","location":"zoo/#2D-2","page":"Zoo","text":"sol = mgb_solve(Zoo.minimal_surface(mg2); verbose=false, tol=1e-3)\nplot(sol); savefig(\"zoo_minsurf_2d.svg\"); nothing  # hide\nclose()  # hide\n\n(Image: )","title":"2D"},{"category":"section","location":"zoo/#3D-2","page":"Zoo","text":"sol = mgb_solve(Zoo.minimal_surface(mg3); verbose=false, tol=1e-3)\nfig = plot(sol); savefig(fig, \"zoo_minsurf_3d.png\"); nothing  # hide\n\n(Image: )","title":"3D"},{"category":"section","location":"zoo/#3.-p-harmonic-maps-(vectorial-p-Laplacian)","page":"Zoo","text":"min int nabla u_F^p + f cdot u  dxqquad uOmega to mathbbR^d\n\nThe full-gradient (Frobenius) p-energy. Plots show u(x), a scalar field.","title":"3. p-harmonic maps (vectorial p-Laplacian)"},{"category":"section","location":"zoo/#1D-3","page":"Zoo","text":"In 1D, u is scalar and this reduces to standard scalar p-Laplace.\n\nsol = mgb_solve(Zoo.p_harmonic(mg1); verbose=false, tol=1e-3)\nplot(sol); savefig(\"zoo_pharm_1d.svg\"); nothing  # hide\nclose()  # hide\n\n(Image: )","title":"1D"},{"category":"section","location":"zoo/#2D-3","page":"Zoo","text":"sol = mgb_solve(Zoo.p_harmonic(mg2); verbose=false, tol=1e-3)\nu_norm = sqrt.(sol.z[:,1].^2 .+ sol.z[:,2].^2)\nplot(sol.geometry, u_norm); savefig(\"zoo_pharm_2d.svg\"); nothing  # hide\nclose()  # hide\n\n(Image: )","title":"2D"},{"category":"section","location":"zoo/#3D-3","page":"Zoo","text":"sol = mgb_solve(Zoo.p_harmonic(mg3); verbose=false, tol=1e-3)\nu_norm = sqrt.(sol.z[:,1].^2 .+ sol.z[:,2].^2 .+ sol.z[:,3].^2)\nfig = plot(sol.geometry, u_norm); savefig(fig, \"zoo_pharm_3d.png\"); nothing  # hide\n\n(Image: )","title":"3D"},{"category":"section","location":"zoo/#4.-Norton–Hoff-power-law-elasticity","page":"Zoo","text":"min int varepsilon(u)_F^p + f cdot u  dx\nqquad varepsilon(u) = tfrac12(nabla u + nabla u^T)\n\nSymmetric-gradient power-law (the physically correct model: linear elasticity at p=2, Norton–Hoff creep for 1 leq p  2). Plots show u(x).","title":"4. Norton–Hoff power-law elasticity"},{"category":"section","location":"zoo/#1D-4","page":"Zoo","text":"Not defined in 1D — symmetric gradient reduces to scalar gradient. Use scalar p-Poisson or Zoo.elastoplastic_torsion instead.","title":"1D"},{"category":"section","location":"zoo/#2D-4","page":"Zoo","text":"sol = mgb_solve(Zoo.norton_hoff(mg2); verbose=false, tol=1e-3)\nu_norm = sqrt.(sol.z[:,1].^2 .+ sol.z[:,2].^2)\nplot(sol.geometry, u_norm); savefig(\"zoo_norton_2d.svg\"); nothing  # hide\nclose()  # hide\n\n(Image: )","title":"2D"},{"category":"section","location":"zoo/#3D-4","page":"Zoo","text":"sol = mgb_solve(Zoo.norton_hoff(mg3); verbose=false, tol=1e-3)\nu_norm = sqrt.(sol.z[:,1].^2 .+ sol.z[:,2].^2 .+ sol.z[:,3].^2)\nfig = plot(sol.geometry, u_norm); savefig(fig, \"zoo_norton_3d.png\"); nothing  # hide\n\n(Image: )","title":"3D"},{"category":"section","location":"zoo/#5.-ROF-total-variation-denoising","page":"Zoo","text":"min_u int nabla u + tfraclambda2(u - f_mathrmdata)^2  dx\n\nRudin–Osher–Fatemi 1992 — the foundational variational model for edge-preserving image denoising. We feed a step-like f_data so the denoised u is non-trivial.","title":"5. ROF total-variation denoising"},{"category":"section","location":"zoo/#1D-5","page":"Zoo","text":"sol = mgb_solve(Zoo.rof(mg1); verbose=false, tol=1e-3)\nplot(sol); savefig(\"zoo_rof_1d.svg\"); nothing  # hide\nclose()  # hide\n\n(Image: )","title":"1D"},{"category":"section","location":"zoo/#2D-5","page":"Zoo","text":"sol = mgb_solve(Zoo.rof(mg2); verbose=false, tol=1e-3)\nplot(sol); savefig(\"zoo_rof_2d.svg\"); nothing  # hide\nclose()  # hide\n\n(Image: )","title":"2D"},{"category":"section","location":"zoo/#3D-5","page":"Zoo","text":"sol = mgb_solve(Zoo.rof(mg3); verbose=false, tol=1e-3)\nfig = plot(sol); savefig(fig, \"zoo_rof_3d.png\"); nothing  # hide\n\n(Image: )","title":"3D"},{"category":"section","location":"zoo/#6.-Two-sided-obstacle","page":"Zoo","text":"min int tfrac12nabla u^2 + f u  dx\nquad textstquad psi_mathrmlower(x) leq u(x) leq psi_mathrmupper(x)\n\nMembrane pinched between an upper and a lower obstacle.","title":"6. Two-sided obstacle"},{"category":"section","location":"zoo/#1D-6","page":"Zoo","text":"sol = mgb_solve(Zoo.two_sided_obstacle(mg1); verbose=false, tol=1e-3)\nplot(sol); savefig(\"zoo_obstacle_1d.svg\"); nothing  # hide\nclose()  # hide\n\n(Image: )","title":"1D"},{"category":"section","location":"zoo/#2D-6","page":"Zoo","text":"sol = mgb_solve(Zoo.two_sided_obstacle(mg2); verbose=false, tol=1e-3)\nplot(sol); savefig(\"zoo_obstacle_2d.svg\"); nothing  # hide\nclose()  # hide\n\n(Image: )","title":"2D"},{"category":"section","location":"zoo/#3D-6","page":"Zoo","text":"sol = mgb_solve(Zoo.two_sided_obstacle(mg3); verbose=false, tol=1e-3)\nfig = plot(sol); savefig(fig, \"zoo_obstacle_3d.png\"); nothing  # hide\n\n(Image: )","title":"3D"},{"category":"section","location":"zoo/#Zoo-module-reference","page":"Zoo","text":"","title":"Zoo module reference"},{"category":"module","location":"zoo/#MultiGridBarrier.Zoo","page":"Zoo","text":"Zoo\n\nA library of convex variational test problems. Each constructor takes a MultiGrid and returns an assembled, closure-free MGBProblem; solve it with mgb_solve(problem; kwargs...). Problem-defining parameters (p, forcing f, boundary data g_u, …) are keyword arguments of the constructor; solver-control parameters (tol, device, …) are passed to mgb_solve.\n\nGPU support\n\nSolve on a GPU by loading the CUDA extension (using CUDA, CUDSS_jll) and passing device = CUDADevice to mgb_solve, e.g. mgb_solve(Zoo.p_harmonic(mg); device = CUDADevice). The problem is assembled on the CPU and moved to the device (and the solution back) by mgb_solve.\n\n\n\n\n\n","title":"MultiGridBarrier.Zoo"},{"category":"method","location":"zoo/#MultiGridBarrier.Zoo.elastoplastic_torsion-Union{Tuple{MultiGrid{T, M_R, G} where {M_R, G<:(Geometry{T, X} where X<:AbstractArray{T, 3})}}, Tuple{T}} where T","page":"Zoo","text":"elastoplastic_torsion(mg; f, g_u, smax, s_init) -> MGBProblem\n\nHencky elasto-plastic torsion of a prismatic bar. State z = (u s), problem\n\nmin int tfrac12nabla u^2 + f u  dx\nquad textstquad nabla u leq textttsmaxtextae\n\nencoded as s geq nabla u^2 (slack inequality) and s leq textttsmax^2 (linear constraint). At the optimum s = nabla u^2, so tfrac12int s in the objective recovers the elastic energy tfrac12intnabla u^2.\n\nReturns an MGBProblem; solve with mgb_solve(problem; kwargs...).\n\nKeyword arguments\n\nf::Function: scalar linear forcing. The dim-dependent default (f = 2 in 1D, 4 in 2D, 16 in 3D) is chosen so that the elastic solution's maxnabla u overshoots smax by roughly 2times, which places the plastic free boundary well inside the domain — the active set (nabla u = s_max) covers approx 25–75 of Omega, showing the elastic / plastic transition clearly.\ng_u::Function = x -> 0: scalar Dirichlet boundary lift for u.\nsmax::Real = 1: yield bound on |∇u|.\ns_init::Real = smax^2/2: feasible initial slack (must lie in (0, smax^2)).\n\n\n\n\n\n","title":"MultiGridBarrier.Zoo.elastoplastic_torsion"},{"category":"method","location":"zoo/#MultiGridBarrier.Zoo.minimal_surface-Union{Tuple{MultiGrid{T, M_R, G} where {M_R, G<:(Geometry{T, X} where X<:AbstractArray{T, 3})}}, Tuple{T}} where T","page":"Zoo","text":"minimal_surface(mg; g_u, s_init) -> MGBProblem\n\nMinimal-surface (Plateau) problem in graph form. State z = (u s), problem\n\nmin int sqrt1 + nabla u^2 dx\n\nwith prescribed Dirichlet boundary trace g_u. The integrand is encoded as s geq sqrt1 + nabla u^2 via the Euclidean-power barrier with a non-trivial affine A, b: the augmented vector Ay + b packs (∇u, 1, 0, s) so the slack inequality s^2 geq nabla u^2 + 1 is the shifted Lorentz cone.\n\nKeyword arguments\n\ng_u::Function: Dirichlet boundary lift. The dim-aware default produces a non-trivial picture: x mapsto tfrac12 x_1^2 in 1D, x mapsto tfrac12(x_1^2 - x_2^2) (saddle) in 2D, and x mapsto tfrac12x^2 in 3D.\ns_init::Real = 10: feasible initial slack (need s^2  nabla u_0^2 + 1).\n\n\n\n\n\n","title":"MultiGridBarrier.Zoo.minimal_surface"},{"category":"method","location":"zoo/#MultiGridBarrier.Zoo.norton_hoff-Union{Tuple{MultiGrid{T, M_R, G} where {M_R, G<:(Geometry{T, X} where X<:AbstractArray{T, 3})}}, Tuple{T}} where T","page":"Zoo","text":"norton_hoff(mg; p, f, g_u, s_init) -> MGBProblem\n\nNorton–Hoff power-law (visco-)elasticity for a vector displacement u  Omega subset mathbbR^d to mathbbR^d:\n\nmin int varepsilon(u)_F^p + f cdot u  dx\nqquad varepsilon(u) = tfrac12(nabla u + nabla u^T)\n\nThe symmetric gradient varepsilon kills rigid rotations, so this is the physically-correct power-law model: linear elasticity at p=2, Norton–Hoff creep / glacier ice / non-Newtonian power-law flow for 1 leq p  2.\n\nState z = (u_1 ldots u_d s) with slack s geq varepsilon(u)_F^p.\n\nImplemented in 2D (d=2) and 3D (d=3); 1D throws an error (in 1D the symmetric gradient reduces to the scalar gradient, so use scalar p-Poisson or elastoplastic_torsion).\n\nKeyword arguments\n\np::Real = 1.5: power-law exponent. Default 1.5 puts us in the non-Newtonian / Norton–Hoff creep regime (1 leq p  2); p = 2 collapses to linear elasticity and is the least interesting case.\nf::Function = x -> ntuple(_->T(0.5), d): vector linear forcing.\ng_u::Function: Dirichlet boundary lift. The dim-aware default is a saddle-shaped trace on the first component ((x_1 x_2, 0) in 2D, (x_1 x_2 x_3, 0, 0) in 3D) — non-affine, so the interior deforms in response without needing strong forcing.\ns_init::Real = 100: feasible initial slack.\n\n\n\n\n\n","title":"MultiGridBarrier.Zoo.norton_hoff"},{"category":"method","location":"zoo/#MultiGridBarrier.Zoo.p_harmonic-Union{Tuple{MultiGrid{T, M_R, G} where {M_R, G<:(Geometry{T, X} where X<:AbstractArray{T, 3})}}, Tuple{T}} where T","page":"Zoo","text":"p_harmonic(mg; p, f, g_u, s_init) -> MGBProblem\n\nThe p-energy for vector-valued maps (the \"vectorial p-Laplacian\"),\n\nmin int nabla u_F^p + f cdot u  dx\nqquad u  Omega to mathbbR^d\n\nwhere nabla u_F^2 = sum_ij (partial u_i  partial x_j)^2 is the Frobenius norm of the gradient matrix. Minimisers are p-harmonic maps (in the flat-target case). Note: this is not elasticity — Frobenius of the full gradient penalises rigid rotations. For Norton–Hoff power-law elasticity, see norton_hoff.\n\nState z = (u_1 ldots u_d s) with slack s geq nabla u_F^p.\n\nKeyword arguments\n\np::Real = 1.5: power-law exponent. Default 1.5 is in the genuinely non-quadratic regime (p = 2 collapses to a decoupled system of scalar Laplacians and is the least interesting case).\nf::Function = x -> ntuple(_->T(0.5), d): vector linear forcing.\ng_u::Function: Dirichlet boundary lift. The dim-aware default is a saddle-shaped trace on the first component, zero on the others ((x_1^2,) in 1D, (x_1 x_2, 0) in 2D, (x_1 x_2 x_3, 0, 0) in 3D), which is non-affine and drives the interior away from zero without needing strong forcing.\ns_init::Real = 100: feasible initial slack.\n\n\n\n\n\n","title":"MultiGridBarrier.Zoo.p_harmonic"},{"category":"method","location":"zoo/#MultiGridBarrier.Zoo.rof-Union{Tuple{MultiGrid{T, M_R, G} where {M_R, G<:(Geometry{T, X} where X<:AbstractArray{T, 3})}}, Tuple{T}} where T","page":"Zoo","text":"rof(mg; f_data, λ, g_u, s_init, r_init) -> MGBProblem\n\nRudin–Osher–Fatemi total-variation denoising of a scalar field f_data:\n\nmin_u int nabla u + tfraclambda2 (u - f_mathrmdata)^2  dx\n\nencoded with state z = (u s r) where s geq nabla u is the TV slack and r geq (u - f_mathrmdata)^2 is the data-fitting slack. The linear objective becomes int (s + tfraclambda2 r)  dx.\n\nKeyword arguments\n\nf_data::Function: scalar noisy input. Default is a smoothed-step signal 05 tanh(5 x_1) along the first coordinate.\nλ::Real = 1: fidelity weight.\ng_u::Function: Dirichlet boundary lift for u. Defaults to f_data so the boundary trace matches the data — otherwise the zero Dirichlet BC fights the data near partial Omega and drags u to zero.\ns_init::Real = 10, r_init::Real = 10: feasible initial slacks.\n\n\n\n\n\n","title":"MultiGridBarrier.Zoo.rof"},{"category":"method","location":"zoo/#MultiGridBarrier.Zoo.two_sided_obstacle-Union{Tuple{MultiGrid{T, M_R, G} where {M_R, G<:(Geometry{T, X} where X<:AbstractArray{T, 3})}}, Tuple{T}} where T","page":"Zoo","text":"two_sided_obstacle(mg; f, g_u, ψ_lower, ψ_upper, s_init) -> MGBProblem\n\nMembrane between an upper and a lower obstacle:\n\nmin int tfrac12nabla u^2 + f u  dx\nquad textstquad psi_mathrmlower(x) leq u(x) leq psi_mathrmupper(x)\n\nEncoded with state z = (u s), slack s geq nabla u^2, and two linear barriers u - psi_mathrmlower  0, psi_mathrmupper - u  0.\n\nKeyword arguments\n\nf::Function: scalar linear forcing. The dim-dependent default (f = 1 in 1D, 2 in 2D, 8 in 3D) drives the elastic solution past the default lower obstacle ψ_lower = -0.1, so the active set covers approx 25–75 of Omega.\ng_u::Function = x -> 0: Dirichlet lift; must lie strictly inside the obstacles.\nψ_lower::Function = x -> -0.1, ψ_upper::Function = x -> 1: obstacles. Default values place the lower obstacle close enough to zero that the default forcing reaches it; the upper obstacle is loose by default.\ns_init::Real = 10: feasible initial slack.\n\n\n\n\n\n","title":"MultiGridBarrier.Zoo.two_sided_obstacle"}]
}
